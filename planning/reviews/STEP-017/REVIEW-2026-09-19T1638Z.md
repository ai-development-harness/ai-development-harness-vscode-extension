# REVIEW STEP-017 — 2026-09-19 16:38Z

**Reviewer role:** reviewer (независимый от автора ADR и FIX-прохода)
**Дополнительные reviewers:** security_reviewer; test_reviewer не запускался,
так как фактический scope остаётся документационным и test surface не менялся
**Verdict:** FAIL
**Reviewed revision:** working tree (uncommitted), `main` @ `3646f5a`

## Scope checked

- Полный контракт `planning/tasks/STEP-017.md`: Scope, Mutation policy,
  Acceptance criteria, Implementation plan и Evidence.
- REQ-005, ADR-004/ADR-006/ADR-007, новый ADR-008, ADR index, architecture
  baseline и planning/requirements projections.
- Закрытие F-001..F-003 из первого review
  `planning/reviews/STEP-017/REVIEW-2026-09-19T1633Z.md`.
- Handoff F-001..F-007 из
  `planning/reviews/STEP-009/REVIEW-2026-09-19T1614Z.md`.
- Фактический working-tree diff. Изменения `src/**`, `tests/**`, dependencies,
  CLI settings и production behavior относятся к ранее reviewed STEP-009 и не
  приписывались STEP-017; review их не изменял.

## Closure предыдущего review

- F-001 закрыт: ADR index теперь помечает ADR-006 как superseded ADR-008 в
  части automatic write flow, а ADR-008 остаётся активным `Accepted` contract.
- F-002 закрыт в ADR-008: current corrective work закрывает F-003 STEP-009
  отсутствием automatic write-spawn; containment требуется лишь при будущем
  отдельном ADR. Однако старый двусмысленный handoff сохранился в task plan и
  architecture baseline — см. новый F-001 ниже.
- F-003 закрыт в основных projection-разделах: architecture содержит явную
  manual mutating branch, а STATUS отражает состоявшийся FAIL STEP-009 и
  STEP-017. Остаточное противоречие architecture debt описано ниже.

## Findings

### F-001 — Medium (architecture/security) — Task plan и architecture debt всё ещё требуют containment от текущего FIX вопреки ADR-008

**Location:** `planning/tasks/STEP-017.md:118-122,134-137`;
`docs/architecture.md:59`; canonical contract
`docs/adr/ADR-008-headless-agent-write-boundary.md:90-92`.

ADR-008 теперь однозначно требует, чтобы текущий `STEP FIX STEP-009` закрыл
риск detached descendants отсутствием automatic write-spawn. Containment и
его regression становятся обязательными только после будущего отдельного ADR,
который вернёт automatic write. Но persisted Implementation plan по-прежнему
передаёт в corrective handoff «POSIX detached-descendant containment» и
относит его implementation/tests к отдельному corrective STEP, а architecture
debt прямо перечисляет «проверяемую platform containment» среди необходимых
изменений текущего `STEP FIX STEP-009`.

**Scenario:** implementer следует task plan и architecture baseline, расширяет
текущий FIX до cgroup/Job Object либо сохраняет/возвращает automatic write по
частичному process-tree evidence без требуемого нового ADR и доказанной
read-boundary.

**Impact:** fail-closed Decision допускает противоположную реализацию; могут
вернуться host-wide read и detached-write риски, ради которых создан STEP-017.
Как минимум corrective scope остаётся неоднозначным.

**Fix direction:** в обоих местах явно указать, что текущий FIX закрывает
F-003 только pre-spawn manual fallback без automatic write-process.
Containment и regressions нужны лишь после отдельного будущего ADR, который
явно разрешит automatic write для конкретной executor/platform combination.

## Acceptance observations

- ADR-008 имеет статус `Accepted`, сравнивает OS containment, informed consent
  и manual fallback, фиксирует current matrix Codex/Claude × POSIX/Windows и
  блокирует automatic write до spawn.
- POSIX `setsid`, Windows Job Object/unsupported platform и отсутствие
  Ctrl+C-гарантии для manual terminal описаны честно.
- Minimal per-executor environment allowlist и запрет вывода secret values
  зафиксированы; ADR-008 содержит все F-001..F-007 STEP-009.
- REQ-005 остаётся незавершённым; index, основной architecture flow, roadmap и
  status projections в остальном согласованы.
- Historical ADR-004/006/007 не изменялись. Все заявленные REQ/ADR/STEP/review
  paths существуют.
- Acceptance о непротиворечивом architecture/handoff не выполнен из-за F-001.

## Verification observations

| Command/check | Exit | Observed |
|---|---:|---|
| `python3 tools/harness/validate.py --mode commit` | 0 | `HARNESS VALIDATION: PASS` для 309 tracked files; только warning об отсутствии staged files. |
| `git diff --check` | 0 | Пробельных ошибок нет. |
| Link existence check | 0 | REQ-005, ADR-004/006/007/008, STEP-009/017 и оба предыдущих review существуют. |
| Scope/provenance check | — | Runtime/test/settings diff уже зафиксирован review STEP-009 и не относится к этому ADR-step. |
| Security review | — | Active ADR безопасно fail-closed, но task/architecture handoff допускает возврат automatic write без нового ADR. |
| Test adequacy review | — | Product tests для documentation-only STEP не требуются; semantic consistency остаётся failing gate. |

## Verdict rationale

Исправления закрыли index и основные projection gaps первого review, а сам
ADR-008 теперь даёт консервативный и проверяемый current contract. Но task plan
и architecture baseline всё ещё направляют текущий corrective FIX к
containment, которое ADR разрешает только после будущего отдельного решения.
Для security-sensitive ADR это не косметическая неточность: два project
sources дают implementer'у противоположный scope.

**Verdict: FAIL.**

## Handoff

Устранить F-001 только в разрешённых documentation/planning файлах, повторить
deterministic gates и выполнить новый независимый `STEP REVIEW STEP-017`.
