# REVIEW STEP-008 — 2026-09-21 07:34Z

**Reviewer role:** reviewer (независимый, не автор реализации)
**Verdict:** FAIL
**Reviewed revision:** working tree после последнего `STEP FIX STEP-008`
**Previous report:** `planning/reviews/STEP-008/REVIEW-2026-09-21T0714Z.md`

## Проверка предыдущего finding

External file refresh после debounce теперь доказан в реальном Extension Host.
Test seam также публикует command IDs и arguments. Однако click-action contract
закрыт частично: тест не сверяет target arguments file items и не требует exact
next command ID.

## Findings

### F-001 — Medium — click regression допускает неверные targets и неверную STEP action

**Location:** `tests/integration/editor.test.js:17-31`,
`src/ui/statusBar.ts:74-78`

Тест проверяет только, что initialization/completion используют `vscode.open`,
но не проверяет их arguments. Поэтому обе кнопки могут открывать один неверный
файл и test останется зелёным, хотя REQ-004 требует открыть соответственно
manifest и `STATUS.md`.

Для next item assertion `startsWith('harness.')` допускает любую Harness-команду.
Fixture создаёт конкретный `STEP-998`, однако тест не связывает показанную
canonical command с exact command ID (`harness.plan`/`harness.fix`/
`harness.implement`). Неверная action при корректном target также пройдёт.

Warnings command проверен exact; отсутствие arguments для string command
соответствует реализации.

**Impact:** основной внешний refresh доказан, но acceptance «клик по каждому
элементу выполняет заявленное действие» всё ещё не защищён от неверного file
target или неверного STEP action.

**Fix direction:** сравнить URI arguments initialization/completion с exact
`.harness/manifest.yaml` и manifest-resolved `sources.status`; для controlled
STEP fixture определить ожидаемую canonical recommendation и проверить exact
next command ID вместе с `['STEP-998']`.

## Verification

- `npm run compile` — exit 0.
- `npm run lint` — exit 0.
- `npm test -- --runInBand` — exit 0, 27 suites, 337 tests.
- `npm run build` — exit 0.
- `XDG_RUNTIME_DIR=/tmp npm run test:integration` — exit 0, 28 tests.
- `python3 .harness/tools/validate.py --mode commit` — exit 0, PASS.
- `git diff --check` — exit 0.

## Verdict rationale

REQ-004 external refresh/debounce evidence теперь присутствует, production
findings остаются закрытыми. Но exact commands/arguments каждого клика, явно
запрошенные предыдущим review, не доказаны полностью. Verdict — FAIL; требуется
`STEP FIX STEP-008` и новый независимый review.
