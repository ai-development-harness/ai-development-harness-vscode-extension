# REVIEW STEP-008 — 2026-09-20 18:03Z

**Reviewer role:** reviewer (независимый, не автор реализации)
**Verdict:** FAIL
**Reviewed revision:** working tree

## Findings

### F-001 — High — игнорируется `project.initialized`

`src/ui/statusBar.ts` показывает готовое состояние для любого валидного manifest.
Для `initialized: false` должны быть скрыты project items и показано состояние
неинициализированного проекта.

### F-002 — High — next item не показывает recommendation

Item не переиспользует `selectNextStep`, не отображает STEP/canonical command и
не имеет empty state. Нужно использовать существующий selector без дублирования
его логики.

### F-003 — High — health count/action не соответствуют REQ-004

Счётчик включает сторонние diagnostics VS Code, а click вызывает roadmap status.
Нужна принадлежность только Harness diagnostics и релевантное action surface.

### F-004 — Medium — scheduler допускает racing refresh

Нужны running/pending state, generation guard после dispose и обработка rejected
refresh; tests должны покрыть overlap.

### F-005 — Medium — не выводится completion percentage

REQ-004 требует процент completion roadmap, а текущий item показывает только count.

### F-006 — Medium — watcher не ограничен Harness artifacts

Глобальный glob затрагивает весь workspace вместо manifest, task directory и
status projection; нужен manifest-resolved watcher и reattach при topology change.

## Verdict rationale

Compile, lint, focused Jest и `git diff --check` зелёные, но High findings
нарушают acceptance REQ-004. Нужен `STEP FIX STEP-008` и свежий independent review.
