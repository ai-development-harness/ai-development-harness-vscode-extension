# REVIEW STEP-009 — 2026-09-19 20:38Z

**Reviewer role:** reviewer (независимый от implementer/FIX-проходов)
**Дополнительные reviewers:** test reviewer (`review.tests: auto`) из-за
последнего FAIL по обязательной Extension Host matrix; отдельный security
reviewer не запускался, потому что последний FIX менял только tests, а
production security boundary после предыдущего security review не изменилась
**Verdict:** PASS
**Reviewed revision:** working tree (uncommitted), `main` @ `3646f5a`

## Scope checked

- Контракт `planning/tasks/STEP-009.md`: Required FIX handoff, Acceptance
  criteria, Verification и актуальная plan revision 2.
- REQ-005/REQ-006 и Accepted ADR-010/ADR-011.
- Фактический путь `src/commands/activation.ts` -> `dispatchViaAgent()` ->
  `HarnessAgentDispatcher`, CTS/Mutation policy pre-validation и отсутствие
  context builder, executor resolution или `spawn`.
- Обе text-command family (`STEP ADD`, `PROJECT QUICK FIX`), отдельные
  pure-normal/hostile/invalid inputs, RU/EN, Output Channel, warning и error
  notification, fake Codex/Claude no-spawn и exact non-text command.
- Последовательность FAIL-review от `REVIEW-2026-09-19T1953Z.md` до
  `REVIEW-2026-09-19T2030Z.md`, focused unit tests, Extension Host regressions и
  полный набор deterministic gates.

## Closure предыдущего review

- F-001 из `REVIEW-2026-09-19T2030Z.md` закрыт: каждый valid text scenario
  сохраняет `errorStart` до реального Command Palette dispatch и требует
  `errorMessages.slice(errorStart)` равным `[]` для обеих family, RU/EN и
  pure-normal/hostile input. Exact `STEP FIX STEP-009` применяет ту же проверку
  на RU и EN.
- Предыдущие findings остаются закрыты: invalid input использует полный
  локализованный blocker; `PROJECT QUICK FIX` покрыт наравне с `STEP ADD`;
  pure-normal input отделён от hostile payload; warning сравнивается с полной
  RU/EN wrapper, а ordinary intent запрещён в каждом проверяемом sink.
- Специализированный test review дал PASS: существенных пробелов в mandatory
  matrix и owning-layer regressions не найдено.

## Findings

Новых blocking или non-blocking findings не обнаружено.

## Acceptance observations

- Valid `STEP ADD` и `PROJECT QUICK FIX` для pure-normal и hostile input на RU/EN
  дают ровно один локализованный неисполняемый descriptor в Output Channel и
  ровно одну полную localized warning wrapper. Error sink остаётся пустым.
- Original intent, нейтральный sentinel, secret-shaped fragments, CR/LF,
  ANSI и Unicode control/format bytes не достигают Output Channel, warning
  notification или i18n params; production label строится без зависимости от
  содержимого intent.
- Empty, whitespace-only и control-only input для обеих text-family на RU/EN
  даёт exact actionable error blocker, не создаёт Output/warning handoff и не
  запускает fake Codex/Claude.
- Команда без free text сохраняет exact `STEP FIX STEP-009` в Output Channel и
  полной RU/EN warning wrapper, не создаёт error notification и не передаёт
  prompt дочернему процессу.
- `HarnessAgentDispatcher` заканчивает dispatch manual fallback после CTS и
  Mutation policy pre-validation. Context builder, executor resolution,
  clipboard/terminal transport и `spawn` из текущего command path недостижимы;
  cancel не управляет пользовательским manual process.

## Verification observations

| Command | Exit | Observed |
|---|---:|---|
| `npm test -- --runInBand tests/unit/api/agentDispatcher.test.ts` | 0 | 1 suite, 10 tests passed. |
| `npm run compile` | 0 | `tsc --noEmit` завершился успешно. |
| `npm run lint` | 0 | `eslint src` без ошибок. |
| `npm test -- --runInBand` | 0 | 26 suites, 252 tests passed. |
| `npm run build` | 0 | esbuild завершился успешно. |
| `npm run test:integration` | 1 | Sandbox не разрешил VS Code runtime socket (`EROFS`). |
| `npm run test:integration` (local runtime access) | 0 | 21 Extension Host scenarios passed. |
| `git diff --check` | 0 | Пробельных ошибок нет. |
| `python3 tools/harness/validate.py --mode commit` | 0 | `HARNESS VALIDATION: PASS` (309 tracked files); warning только об отсутствии staged files. |
| Specialized test review | PASS | Mandatory RU/EN/family/input/sink/no-spawn matrix и focused unit regression полны. |

## Verdict rationale

Последний regression gap закрыт, все предыдущие findings остаются закрытыми,
production behavior соответствует manual-only safe handoff, а обязательная
RU/EN Extension Host matrix теперь контролирует все три UI sink для valid и
invalid paths. Deterministic gates зелёные; correctness, security, localization,
compatibility и meaningful test gaps не обнаружены.

**Verdict: PASS.**

## Handoff

STEP-009 может быть завершён и синхронизирован в projection-файлах отдельным
status/documentation проходом. Следующая рекомендуемая команда после закрытия
STEP — `GIT CHECK`.
