# REVIEW STEP-019 — 2026-09-19 18:15Z

**Reviewer role:** reviewer (третий свежий независимый review после FIX)
**Security review:** выполнен из-за risk flag `security-sensitive`
**Test review:** не запускался: STEP меняет architecture/requirements/planning,
а production tests находятся вне Mutation policy
**Verdict:** PASS
**Reviewed revision:** working tree (uncommitted), `main` @ `3646f5a`

## Scope checked

- Latest FAIL `REVIEW-2026-09-19T1810Z.md` и remediation F-001/F-002/F-003.
- ADR-004/ADR-010 supersession metadata, ADR index и сохранение границ
  ADR-007/ADR-008/ADR-009.
- Capability matrix Codex/Claude по macOS, Linux, WSL2 и native Windows,
  primary links и разделение vendor capability от product-specific proof.
- REQ-005, architecture baseline, STEP-009/STEP-019 и roadmap/status
  projections.
- Durable security handoff production finding F-003 в отдельный
  `STEP FIX STEP-009`.
- Фактический cumulative working-tree diff; production code review не меняет.

## Closure предыдущего review

### F-001 — закрыт

- `docs/adr/ADR-004-agent-invocation-mechanism.md:7` и
  `docs/adr/ADR-010-manual-agent-handoff-mvp.md:6` взаимно и одинаково
  фиксируют supersession Decision 1–7 ADR-004 только для automatic invocation
  из Extension Host в MVP.
- `docs/adr/README.md:28,34` отражает ту же scoped supersession. Historical
  observations ADR-004 сохранены, но больше не могут читаться как active MVP
  contract для spawn, Claude fallback, stdin, cancel или permission mode.

### F-002 — закрыт

- `docs/adr/ADR-010-manual-agent-handoff-mvp.md:91-105` задаёт однозначный
  legend и раздельные столбцы: scoped read/write/structured result являются
  vendor claims, cancellation/minimal environment — product evidence.
- Для документированных read/write свойств явно указано отсутствие product
  proof; native Windows Claude помечен `Недоступно` для sandbox read/write.
  Ни одна строка не получает automatic verdict, потому что обязательные
  cancellation и environment criteria не доказаны.
- Все шесть primary links доступны (HTTP 200 после redirect) и подтверждают
  заявленный уровень: platform sandbox/path permissions/structured output,
  но не полный Extension Host lifecycle и detached-descendant containment.

### F-003 — закрыт как durable owning-layer handoff

- `planning/tasks/STEP-009.md:38-46` содержит отдельный обязательный FIX
  handoff со ссылкой на F-003
  `planning/reviews/STEP-019/REVIEW-2026-09-19T1801Z.md`.
- Handoff требует единую bounded redaction/control-character sanitization
  canonical command до Output Channel и i18n notification, а также unit и
  Extension Host regressions для token-shaped input, CR/LF и ANSI/control
  bytes и отсутствие spawn.
- Фактический unsafe путь всё ещё виден в
  `src/api/agentDispatcher.ts:94-96`; его production remediation корректно
  остаётся вне Mutation policy STEP-019 и принадлежит `STEP FIX STEP-009`.

## Findings

Новых blocking или non-blocking findings не обнаружено.

## Security observations

- ADR-010 выбирает fail-closed manual-only contract и не расширяет filesystem,
  process, environment или credential boundaries Extension Host.
- Vendor sandbox claims не подменяют product evidence: matrix оставляет
  process-tree cancellation и minimal environment недоказанными на всех
  комбинациях и поэтому не разрешает automatic executor.
- ADR-007/ADR-008/ADR-009 остаются действующими: context bundle, `cwd` и
  consent не считаются containment; extension не становится владельцем CLI
  credentials.
- Current production adapter всё ещё содержит историческую automatic
  поверхность и unsanitized manual command, но default capability gate не
  разрешает current Codex/Claude read spawn, write path завершается до spawn,
  а весь runtime drift честно оставлен заблокированному STEP-009. STEP-019 не
  выдаёт это за уже завершённую implementation.

## Acceptance observations

- Для каждой рассмотренной executor/platform combination есть verdict по пяти
  критериям и итог `manual only`; уровни vendor capability и product proof не
  смешаны.
- ADR-010 однозначно пересматривает REQ-005 до manual handoff и scoped
  supersedes все extension-owned automatic Decisions ADR-004, не переписывая
  historical evidence.
- REQ-005, architecture, ADR index, STEP-009 и projections не обещают
  automatic lifecycle. STEP-009 остаётся `Заблокировано` до отдельного FIX и
  review.
- Remediation F-001/F-002/F-003 меняла только разрешённые documentation/
  planning artifacts; существующий cumulative `src/**`/`tests/**` diff
  относится к ранее reviewed STEP-009.

## Verification observations

| Command/check | Exit | Observed |
|---|---:|---|
| `python3 tools/harness/validate.py --mode commit` | 0 | `HARNESS VALIDATION: PASS` для 309 tracked files; только warning об отсутствии staged files. |
| `git diff --check` | 0 | Пробельных ошибок нет. |
| Primary URL check | 0 | Все 6 ссылок ADR-010 возвращают HTTP 200 после redirect. |
| Supersession check | — | ADR-004, ADR-010 и index одинаково ограничивают Decisions 1–7 automatic invocation из Extension Host в MVP. |
| Matrix check | — | Vendor read/write/result отделены от отсутствующего product cancellation/environment proof; все строки остаются `manual only`. |
| F-003 handoff check | — | STEP-009 содержит source review, owning layer, обязательные unit/Extension Host regressions и no-spawn expectation. |
| Scope/provenance check | — | STEP-019/FIX не добавляли production code, tests, dependencies, CLI configuration или secret values. |

## Verdict rationale

Все findings предыдущего review закрыты. ADR-004/ADR-010 больше не оставляют
активного automatic loophole, capability matrix воспроизводимо различает
vendor claim и product evidence, а production security finding F-003 получил
durable owning-layer handoff без нарушения document-only scope STEP-019.
Manual-only решение fail-closed, traceability и projections согласованы,
обязательные deterministic gates пройдены.

**Verdict: PASS.**

## Handoff

STEP-019 может быть завершён. Следующая команда — `STEP FIX STEP-009`:
согласовать historical task/runtime surface с manual-handoff REQ-005, закрыть
F-003 sanitization в owning layer с unit и Extension Host regressions, затем
выполнить новый независимый `STEP REVIEW STEP-009`.
