# REVIEW STEP-004 — 2026-09-17T2200

**Verdict:** FAIL
**Task:** `planning/tasks/STEP-004.md` (Plan revision 1)
**Diff scope:** `src/locales/{ru.json,en.json,i18n.ts,activation.ts}`, `tests/unit/locales/i18n.test.ts`, `tests/integration/i18n.test.js`, `tests/fixtures/workspace/.gitkeep` (новые); `package.nls.json`, `package.nls.en.json` (новые); `src/extension.ts`, `package.json` (`contributes.commands`), `tsconfig.json` (`resolveJsonModule`), `docs/architecture.md`, `docs/development.md`, `.vscode-test.mjs` (`workspaceFolder`), `.gitignore` (изменённые).

## Метод

Два независимых read-only review, запущенных параллельно в отдельных агентах без доступа к контексту реализации, каждый самостоятельно перечитал task/ADR/REQ/код/тесты и сам перезапустил verification-команды (не доверяя тексту `## Evidence`):

1. **Correctness/architecture reviewer** — соответствие ADR-001/REQ-006, mutation policy, error handling, regression risk.
2. **Test reviewer** — адекватность и содержательность тестового покрытия относительно заявленного в Test strategy/Acceptance criteria поведения.

## Reviewer 1 (correctness/architecture) — FAIL

Реальный прогон verification (не принято на веру): `npm run compile` (0), `npm run lint` (0), `npm run build` (0), `npm test` (41/41 passed), `npm run test:integration` (реальный VSCode `1.138.0` linux-x64, headless без Xvfb, `4 passing`; после прогона `git status` чист). Все числа совпадают с Evidence.

Mutation policy соблюдена — diff (`git diff -- . ':!planning/tasks/STEP-004.md'`) ограничен ровно заявленным списком, `src/parser/**` не тронут. Обоснование исключения `.project/harness-config.json` из ADR-001 признано корректным (ADR-001 регулирует пути протокола из манифеста, а не произвольные extension-owned файлы). `resolveLanguage`/`translate`/`readHarnessConfig`/`writeHarnessConfig` не бросают исключений на реалистичных входах. Гонки между чтением сохранённого языка и первым использованием сервиса нет — `activateI18n` реально `await`-ит загрузку конфига до `return service`, `extension.ts` `await`-ит `activateI18n`. `package.nls.json`/`package.nls.en.json` синтаксически валидны и корректно связаны через `%harness.command.changeLanguage.title%`. Regression risk: STEP-002/STEP-003 не затронуты.

Findings:

- **F-001 [Blocking]** `src/locales/activation.ts:42-43,58-59` — оба сообщения `vscode.window.showWarningMessage(...)` (ошибка чтения/записи `.project/harness-config.json`) захардкожены на русском, хотя `service.t()` доступен в той же функции `activateI18n`. Given пользователь работает на EN (`resolveLanguage` вернул `'en'`), When конфиг повреждён/недоступен для записи, Then пользователь видит предупреждение целиком на русском. Прямое нарушение собственного `## Mutation policy → Forbidden` этого STEP («Хардкод текста напрямую в UI-коде вместо использования сервиса — закладывается как conventions для последующих STEP») и требования REQ-006 про локализацию «ошибок с подсказками». Причина по существу: `translate()` не поддерживает интерполяцию параметров (`configPath`, `error.message`), поэтому статичный словарный ключ не мог вместить сообщение как есть — структурный пробел, который стоит закрыть до того, как STEP-005 столкнётся с той же потребностью.
- **F-002 [Non-blocking]** `src/locales/i18n.ts:80-86` — `readHarnessConfig` возвращает только `{language}`, отбрасывая остальные поля объекта уже на чтении. Заявление PLAN «unknown-поля не читаются, но и не удаляются при перезаписи» по факту неверно на уровне чтения (см. также Reviewer 2, F-001 — та же находка, независимо подтверждена эмпирически).
- **F-003 [Non-blocking]** `src/locales/i18n.ts:62-67` — любая fs-ошибка чтения (EACCES, EISDIR и т.д.), не только «файл отсутствует», маскируется под `ok({})` без warning — снижает observability, но не нарушает Acceptance criteria этого STEP.

## Reviewer 2 (test coverage) — FAIL

Реальный прогон (независимо от Reviewer 1): `npm test` — 41/41. `npx jest --coverage --collectCoverageFrom='src/locales/**/*.ts'`: `i18n.ts` 94.59% stmts / 79.16% branch / 100% funcs; `activation.ts` отсутствует в отчёте (не инструментируется вне Extension Host — ожидаемо по PLAN). `npm run test:integration` — `4 passing`, прогнан дважды для проверки идемпотентности: оба раза чисто, `suiteTeardown` корректно удаляет генерируемый `.project/`, скрытых межтестовых зависимостей не по назначению не найдено (зависимость теста 3 от теста 2 — документированная, намеренная).

Тесты не декоративные — assertions проверяют конкретные значения (fallback-направления, round-trip конфига, реальные ключи словарей, содержимое persisted-файла).

Findings:

- **F-001 [Blocking]** `src/locales/i18n.ts:80-86` / `src/locales/activation.ts:54-56` — эмпирически подтверждено (throwaway-тест, не закоммичен): запись `{"language":"ru","futureField":"keepme"}`, затем цикл read→merge→write, который использует `activation.ts` при `harness.changeLanguage`, даёт на выходе файл, содержащий **только** `{"language":"en"}` — `futureField` тихо удалён. Это прямое опровержение явной гарантии `## Implementation plan → Data/API implications` («unknown-поля не читаются, но и не удаляются при перезаписи»), и ни один тест (unit/integration) не проверяет сценарий «конфиг с дополнительными полями переживает запись» — ровно тот сценарий, который STEP-004.md сам обещает поддерживать. Совпадает по существу с F-002 Reviewer 1, независимо подтверждено эмпирическим прогоном.
- **F-002 [Non-blocking]** `src/locales/i18n.ts:93-103` — error-path `writeHarnessConfig` (fs.mkdir/fs.writeFile failure) — 0% coverage, branch на строке 101 никогда не входит.
- **F-003 [Non-blocking]** `src/locales/i18n.ts:80` — ветка «валидный JSON, но не объект» (null/массив/строка/число) не протестирована.
- **F-004 [Non-blocking]** `src/locales/i18n.ts:82` — ветка «объект есть, но `language` отсутствует/невалиден» (например `{}` или `{language:"fr"}`) не протестирована.
- **F-005 [Non-blocking]** `src/locales/activation.ts` — путь автоопределения без сохранённого конфига, оба `showWarningMessage`-пути и повторный вызов `harness.changeLanguage` в одной сессии не протестированы; ни один из них не обещан Test strategy/Acceptance criteria явно (ветка «нет workspace»/интерактивный QuickPick — сознательно и корректно отмечены как out of scope в самом task-файле).
- **F-006 [Non-blocking]** `src/locales/i18n.ts:76,101` — fallback `String(error)` для не-`Error` исключений не протестирован; низкий реальный риск.
- **Наблюдение (не оценено как finding):** прозa `## Implementation plan → Test strategy` утверждает, что повреждённый JSON деградирует до `ok({})`, но фактическая реализация возвращает `err({kind:'invalid-json',...})` — и тест верно проверяет именно это фактическое поведение. Функционально не проблема (Acceptance criteria не нарушены), но текст плана и код расходятся — стоит поправить одной строкой при FIX.

## Синтезированный вердикт

**FAIL.** Оба независимых ревьюера пришли к blocking-выводу, и по существу это одна и та же корневая находка, подтверждённая дважды независимо (чтением кода и эмпирическим прогоном): `readHarnessConfig` отбрасывает неизвестные поля конфигурационного файла, что прямо опровергает явную гарантию расширяемости, зафиксированную в `## Implementation plan → Data/API implications` самого STEP-004. Plus отдельная, не пересекающаяся находка Reviewer 1: сообщения об ошибках в `activation.ts` захардкожены на русском в обход только что введённого i18n-сервиса — прямое нарушение `## Mutation policy → Forbidden` этого же STEP. Оба finding конкретны, воспроизводимы и не являются косметикой — они показывают расхождение между тем, что STEP-004 сам объявляет как свой контракт, и тем, что фактически реализовано.

Non-blocking findings (Reviewer 1 F-003; Reviewer 2 F-002..F-006) — реальный, но не обещанный контрактом test/observability debt; рекомендуется закрыть дешёвые из них (F-002..F-004 Reviewer 2) в том же FIX-цикле.

## Handoff

`FIX STEP-004`:

1. **Обязательно** — `src/locales/activation.ts`: заменить оба хардкод-сообщения `showWarningMessage` на локализованные через `service.t()`; расширить `translate`/словари механизмом подстановки параметров (`configPath`, `error.message`) — новый структурный элемент API, которым позже воспользуется STEP-005/STEP-010.
2. **Обязательно** — `src/locales/i18n.ts`: `readHarnessConfig` должен сохранять неизвестные поля прочитанного объекта (не только `language`), чтобы `writeHarnessConfig`-merge в `activation.ts` реально не терял данные; добавить тест на цикл read→merge→write с дополнительным полем.
3. Рекомендуется в этом же цикле: тест на error-path `writeHarnessConfig` (Reviewer 2 F-002), тесты на «не объект» и «объект без `language`» (F-003/F-004); одна строка правки в `## Implementation plan → Test strategy` (описание поведения при повреждённом JSON — фактически `err(...)`, не `ok({})`).
4. Production-код и тесты после фикса — новый `REVIEW STEP-004`.
