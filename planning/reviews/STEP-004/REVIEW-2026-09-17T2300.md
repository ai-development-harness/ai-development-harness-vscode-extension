# REVIEW STEP-004 — 2026-09-17T2300

**Verdict:** PASS
**Task:** `planning/tasks/STEP-004.md` (Plan revision 1, после `FIX STEP-004`)
**Diff scope с прошлого review:** `src/locales/{i18n.ts,activation.ts,ru.json,en.json}`, `tests/unit/locales/i18n.test.ts`, одна фраза в `planning/tasks/STEP-004.md → Implementation plan → Test strategy`. Остальной diff STEP-004 (см. `REVIEW-2026-09-17T2200.md`) не менялся.

## Метод

Два независимых read-only re-review, запущенных параллельно в отдельных агентах без доступа к контексту FIX, каждый самостоятельно перечитал предыдущий FAIL-отчёт, код после фикса и сам перезапустил verification (не доверяя `## FIX STEP-004` Evidence).

1. **Correctness/architecture reviewer** — закрытие F-001 Reviewer 1/F-002 Reviewer 1/F-001 Reviewer 2 по существу, отсутствие новых дефектов, mutation policy FIX-цикла.
2. **Test reviewer** — закрытие F-001 Reviewer 2 (Blocking) построчной проверкой assertions, статус F-002..F-006, новые test gaps от самого фикса.

## Reviewer 1 (correctness/architecture) — PASS

Реальный прогон: `npm run compile` (0), `npm run lint` (0), `npm run build` (0), `npm test` (50/50), `npm run test:integration` (реальный VSCode `1.138.0` headless, `4 passing`, `git status` чист после прогона).

Подтверждено построчным чтением и независимой эмпирической проверкой (throwaway-скрипты, не закоммичены):

- **F-001 Reviewer 1 (хардкод RU в `showWarningMessage`) закрыт.** Оба сообщения в `src/locales/activation.ts` теперь идут через `service.t('harness.config.readError'|'harness.config.writeError', {path, message})`; ключи присутствуют и симметричны в `ru.json`/`en.json`. Единственная оставшаяся русская строка (`throw new Error('getI18nService() вызван до activateI18n()')`) — программная ошибка порядка активации, не user-facing текст, вне Forbidden.
- **F-001 Reviewer 2 / F-002 Reviewer 1 (потеря неизвестных полей) закрыт.** `readHarnessConfig` возвращает `{ ...parsed }` с точечной нормализацией `language`; эмпирически проверено на двойном round-trip (ru→en→ru) с вложенным объектом и произвольным полем — всё пережило обе записи. `HarnessConfig` (`[key: string]: unknown`) согласован с `writeHarnessConfig`.
- Механизм интерполяции `translate()` проверен на дефекты (значение параметра, само содержащее `{placeholder}`, не вызывает повторной интерполяции; отсутствующий ключ и отсутствующий параметр — без исключений).
- Mutation policy FIX-цикла: diff ограничен `src/locales/**`, `tests/unit/locales/i18n.test.ts`, одной фразой в `STEP-004.md`; `src/parser/**` не тронут; диапазон файлов STEP-004 в целом идентичен первому review.
- Новых дефектов/регрессий не найдено.

Информационно (не finding, не blocking): untracked `.vscode/extensions.json`/`.vscode/settings.json` — персональные IDE-настройки, не относятся к STEP-004, не коммитить вместе с ним.

## Reviewer 2 (test coverage) — PASS

Реальный прогон: `npm test` — 50/50 (было 41/41 до фикса). `npx jest --coverage --collectCoverageFrom='src/locales/**/*.ts'`: `i18n.ts` — 100% stmts / 93.1% branch / 100% funcs / 100% lines (было 94.59%/79.16%/100%/94.59%); непокрытые строки 91/121 — обе non-Error fallback (F-006, некритично). `npm run test:integration` — `4 passing`, прогнан дважды для идемпотентности, оба раза чисто.

Построчная проверка `tests/unit/locales/i18n.test.ts:117-131` ("неизвестные поля переживают цикл read→merge→write") подтвердила: тест воспроизводит ровно ту же последовательность, что `activation.ts → setLanguage`, и проверяет точное итоговое значение (`{language:'en', futureField:'keepme'}`), не просто отсутствие исключения — F-001 (Blocking) закрыт по существу, не косметически.

F-002 (writeHarnessConfig error-path), F-003 (не-объектный JSON), F-004 (объект без/с невалидным `language`) — закрыты содержательными, не декоративными тестами (реальная fs-ошибка через файл на месте директории, не мок). F-005 (пути `activation.ts`)/F-006 (non-Error fallback) сознательно не закрыты — не были обещаны контрактом, консистентно с архитектурным решением STEP. Наблюдение про прозу Test strategy — закрыто.

Новые findings (обе non-blocking, informational): интерполяция плейсхолдеров протестирована только на синтетических словарях, не сквозным образом на реальных строках `harness.config.readError`/`writeError`; поведение `params` с ключом без плейсхолдеров не тестируется явно (тривиально корректно по коду). Не рекомендован отдельный FIX-цикл.

## Синтезированный вердикт

**PASS.** Оба blocking finding предыдущего review (`REVIEW-2026-09-17T2200.md`) устранены на уровне кода, независимо подтверждены двумя ревьюерами через построчное чтение и собственные эмпирические проверки, а не приняты на веру из `## FIX STEP-004`. Все verification-команды из `## Verification sequence` реально зелёные. Новых blocking-находок нет. Оставшийся non-blocking technical debt (Reviewer 1 F-003 из первого review; F-005/F-006 test coverage; интерполяция на реальных строках) не противоречит явно заявленному контракту STEP-004 и не блокирует закрытие.

## Handoff

Acceptance criteria доказаны, Evidence зафиксирован, mutation policy соблюдена, deterministic verification успешна → `Status → Выполнено`.
