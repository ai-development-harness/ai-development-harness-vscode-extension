# REVIEW STEP-003 — 2026-09-17T2245

**Verdict:** FAIL
**Task:** `planning/tasks/STEP-003.md` (Plan revision 1)
**Diff scope:** `src/parser/**`, `tests/unit/parser/**`, `tests/fixtures/**` (новые); `package.json`/`package-lock.json` (+`yaml`), `tsconfig.json` (+`compilerOptions.types`), `docs/development.md` (1 строка), `planning/PLAN.md`, `planning/STATUS.md` (изменённые).

## Метод

Два независимых read-only review, запущенных параллельно в отдельных агентах без доступа к контексту реализации, каждый самостоятельно перечитал task/ADR/REQ/код/тесты и сам перезапустил verification-команды (не доверяя тексту `## Evidence`):

1. **Correctness/architecture reviewer** — соответствие ADR-001/ADR-002, correctness, regressions, mutation policy, acceptance criteria.
2. **Test reviewer** — адекватность и содержательность тестового покрытия относительно заявленного поведения.

## Reviewer 1 (correctness/architecture) — PASS

Проверенные verification-команды (реальные exit codes, включая дополнительный самостоятельный прогон): `npm run compile` (0), `npm run lint` (0), `npm test` (20/20 passed), `npm run build` (0). Дополнительно самостоятельно прогнал парсер на **всех** реальных `STEP-001..013.md`, `ADR-003`, `ADR-004`, полном `SPEC.md`, `EXECUTION_PROTOCOL.md` — 0 warnings/exceptions. Эмпирически подтвердил заявленный tsconfig-дефект STEP-002 (временный откат `types: ["node","jest"]` реально ломает компиляцию).

Fixtures побайтово идентичны исходникам (10/10 сверено `diff`). Mutation policy соблюдена — изменения ровно там, где задокументированы три коррекции контракта (`yaml` dependency, `tsconfig.types`, одна строка `docs/development.md`) плюс рутинные обновления `PLAN.md`/`STATUS.md`. ADR-001/ADR-002 соблюдены (пути только из манифеста, labeled markdown, раздельные RU/EN label-карты). Все 4 acceptance criteria подтверждены построчно. `Result<T,E>` contract — скрытых throw на реальных данных не найдено.

Findings (оба **non-blocking**):

- **F-007 [Low]** `splitSections` (`src/parser/markdownParser.ts`) не пропускает fenced code blocks — строка вида `# comment` внутри ``` ``` ``` в будущем документе сломает границы секций. Не проявляется ни на одном реальном файле репозитория сегодня.
- **F-008 [Low]** `ExecutionProtocolCommand.name` для многословных заголовков команд (`` `ADD STEP: <описание>` ``, `QUICK FIX`, `FIND SKILL`, `INSTALL SKILL`, `CREATE SKILL`) включает весь текст плейсхолдера, а не канонический токен — потребует нормализации в будущем STEP-005. Не нарушает acceptance criteria STEP-003.

## Reviewer 2 (test coverage) — FAIL

Тесты не декоративные: используются реальные fixtures, assertions проверяют конкретные значения (все REQ-001..010 с корректной traceability, оба `### Вариант A/Б` ADR, RU/EN смешение меток, edge case `` `COMMIT` / `COMMIT: <подсказка>` `` явно протестирован). `npx jest --coverage --collectCoverageFrom='src/parser/**/*.ts'`: 20/20 тестов, branch coverage `yamlParser.ts` 65.1%, `markdownParser.ts` 62.85%, `executionProtocol.ts` 92.3%.

Findings:

- **F-001 [Blocking]** Деградационные ветки `parseAdrFile` (`src/parser/markdownParser.ts`, warnings на отсутствие root-меток Status/Date/Deciders/Supersedes/Superseded by, отсутствие секции `Alternatives considered`, отсутствие секции `Traceability`) имеют **0% покрытия**. В `tests/fixtures/adr/` нет ни одного намеренно повреждённого ADR (в отличие от `STEP-malformed.md` для STEP), и ни один тест не подаёт в `parseAdrFile` неполный контент. Это прямое расхождение с явным обещанием `## Implementation plan → ### Test strategy` STEP-003 («Негативные/деградационные кейсы» для всех парсеров) и с риском, зафиксированным в `ADR-002 → Compatibility` (парсер должен деградировать на неполном/будущем формате, а не падать) — для ADR это утверждение не подтверждено ни одним тестом.
- **F-002 [Blocking]** Деградация **внутри** одной REQ-записи (`parseReqSpec`, отсутствие меток Статус/Приоритет/Источник или подсекций Requirement/Rationale/Acceptance/Traceability у конкретного REQ-блока) не покрыта. Существующий негативный тест проверяет только другую ветку — «во всём файле нет ни одного REQ-заголовка».
- **F-003 [Non-blocking]** `STEP-malformed.md` покрывает только 3 из ~9 warning-веток `parseStepFile` (нет: отсутствие корневых меток Статус/Type/Приоритет/Фаза; отсутствие целиком секций Mutation policy/Implementation plan/Review status; отсутствие целиком Goal/Context/Evidence/Blocker — сейчас fixture проверяет только «Goal пустая», это другая ветка, чем «секции нет вообще»).
- **F-004 [Non-blocking]** `yamlParser`: ветка «валидный YAML, но не объект» (`kind: 'invalid-yaml', message: 'документ не является объектом'`) и ветка «секция есть, но конкретное вложенное поле отсутствует» (`MissingRequiredField` протестирован только на уровне отсутствующей секции целиком, не вложенного поля) не протестированы.
- **F-005 [Non-blocking]** `parseExecutionProtocol('текст без заголовков')` (ветка `missing-heading` при непустом контенте) не протестирована — аналогичная ветка для `parseStepFile`/`parseReqSpec` покрыта.
- **F-006 [Non-blocking, design note]** «Секция найдена, но 0 элементов извлечено» нигде не даёт warning (только полное отсутствие секции) — не баг, но осознанность этого выбора не задокументирована и не протестирована ни в одну сторону.

## Синтезированный вердикт

**FAIL.** F-001 и F-002 — конкретные, воспроизводимые находки (подтверждены coverage-прогоном и построчным чтением кода обоими независимыми ревьюерами), не косметика: они показывают, что архитектурная гарантия «явная деградация вместо падения» (ADR-001/ADR-002), которую сам STEP-003 обещает проверить тестами по всем трём типам сущностей (`### Test strategy`), фактически подтверждена только для manifest и STEP, но не для ADR (0%) и лишь частично для REQ. Это ровно тот класс «meaningful test gaps», который протокол (`EXECUTION_PROTOCOL.md §10.3`) явно относит к блокирующим находкам review, а не к cosmetic-only.

F-003..F-008 не блокируют, но рекомендуется закрыть в том же FIX-цикле как дешёвые дополнения к уже существующим test-файлам (кроме F-007/F-008, которые можно осознанно отложить с обоснованием, если FIX-исполнитель сочтёт нужным).

## Handoff

`FIX STEP-003` — добавить тесты на F-001 (ADR degradation fixture + тест) и F-002 (REQ inner-block degradation тест) как обязательные; по возможности в этом же цикле закрыть F-003..F-006. Production-код парсера менять не требуется — сами findings про test coverage, не про баг поведения на реальных данных.
