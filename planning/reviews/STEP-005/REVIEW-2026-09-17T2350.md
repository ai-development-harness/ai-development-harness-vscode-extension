# REVIEW STEP-005 — 2026-09-17T2350

**Verdict:** PASS
**Task:** `planning/tasks/STEP-005.md` (Plan revision 1)
**Diff scope:** без изменений относительно предыдущего цикла, плюс `FIX STEP-005`: `src/commands/{preDispatch,projectStatus,stepPicker}.ts` (точечно изменены), `src/locales/{ru.json,en.json}` (новый ключ `harness.error.missingDependency`), `tests/unit/commands/{preDispatch,projectStatus}.test.ts` (новые тесты на F-001).

## Метод

Независимый read-only review второго цикла: перечитаны оба finding предыдущего `REVIEW-2026-09-17T2330.md` (FAIL) и фактический diff `FIX STEP-005`, без доверия тексту `## Evidence` / `## FIX STEP-005`.

Реальный прогон (не принято на веру):

- `npm run compile` — 0 ошибок.
- `npm run lint` — 0 ошибок/warnings.
- `npm run build` — `esbuild: build complete.`
- `npm test` — `Test Suites: 7 passed, 7 total`, `Tests: 86 passed, 86 total` (совпадает с Evidence; было 82 до FIX).
- `npm run test:integration` — реальный VSCode `1.138.0` linux-x64, headless — `6 passing`, без регрессии. `git status` после прогона чист.
- `grep` по `checkHardDependencies`/`resolveDependencySteps` — единственный production call site (`preDispatch.ts` → `runPreDispatchChecks`), сигнатура использована консистентно везде, устаревших вызовов не осталось.

**Эмпирическая переверификация F-001** (throwaway-тест, не закоммичен, тот же вход, что дал `{"ok":true}` в предыдущем цикле): `target.dependsOn = ['STEP-999']`, `allSteps = []` → `resolveDependencySteps` → `[]` → `checkHardDependencies(['STEP-999'], [])` → **`{"ok":false,"messageKey":"harness.error.missingDependency","params":{"step":"STEP-999"}}`**. Дефект закрыт фактически, не только по тексту диффа.

## F-001 [Blocking] — закрыт

`checkHardDependencies` (`src/commands/preDispatch.ts:37-49`) теперь принимает полный `dependsOn` целевого STEP (не предварительно отфильтрованный список), резолвит каждый id по индексу `dependencySteps` и блокирует новым `harness.error.missingDependency`, если id не резолвится вовсе — до проверки `unmetDependency`. `runPreDispatchChecks` обновлён (`ctx.targetStep.dependsOn` вместо только `ctx.dependencySteps`). Симметрично поправлен `computeProjectStatus.unmetDependency` (`projectStatus.ts:29-39`) — недостижимая зависимость теперь тоже в списке.

Тесты не декоративны: `preDispatch.test.ts` содержит прямой unit-тест на `checkHardDependencies` с dangling id и end-to-end тест на `runPreDispatchChecks`, воспроизводящий ровно сценарий из review (`STEP-1` с переопределённым `dependsOn: ['STEP-999']`); `projectStatus.test.ts` — аналогичный тест на `computeProjectStatus`. Прежний тест `resolveDependencySteps` («не резолвит id без файла») сохранён с уточнённым описанием — сама эта функция по контракту только резолвит найденное, обнаружение и блокировка нерезолвленного id теперь явно принадлежат `checkHardDependencies` — разделение ответственности объяснено, не скрыто.

Проверено отсутствие обратной регрессии: сценарий «все зависимости выполнены» и «есть невыполненная, но существующая зависимость» по-прежнему проходят/блокируются корректно (существующие тесты `checkHardDependencies`/`runPreDispatchChecks` актуализированы под новую сигнатуру и зелены).

## F-002 [Non-blocking] — закрыт

`stepPicker.ts:22-25` — `path.join` заменён на `path.posix.join` при построении glob для `findFiles`. Минимальное, точечное изменение; на POSIX (среда прогона) поведение идентично `path.join` — подтверждено неизменным результатом `test:integration` (`6 passing`, включая реальный вызов `STATUS PROJECT`, который внутри использует `listStepFiles`). Эмпирическая проверка на Windows недоступна в этом окружении — как и в предыдущем review, помечено PLAUSIBLE-fix, не CONFIRMED-on-Windows, но это не блокирует PASS: находка изначально была non-blocking, а исправление корректно по существу (glob-паттерны VSCode всегда POSIX-стиль независимо от хоста — задокументированное поведение API).

## Новых находок нет

Повторное прочтение всего diff (включая файлы, не тронутые FIX — `activation.ts`, 11 модулей команд, `agentDispatcher.ts`, `baseCommand.ts`, `nextStepSelector.ts`) не выявило новых дефектов сверх уже закрытых. Mutation policy соблюдена, включая обе задокументированные точечные коррекции (`.gitignore`, `tests/integration/i18n.test.js`).

## Синтезированный вердикт

**PASS.** Оба finding предыдущего цикла (F-001 blocking, F-002 non-blocking) закрыты по существу, подтверждено независимым чтением кода и эмпирическим воспроизведением (не только повторным прогоном существующих тестов). Deterministic verification (compile/lint/build/test/test:integration) успешна. STEP-005 закрывается: `Status → Выполнено`.

## Синхронизация проекций (§25/§10.9 `EXECUTION_PROTOCOL.md`)

- `planning/tasks/STEP-005.md` — `Статус: Выполнено`; `Review status` обновлён.
- `planning/PLAN.md` — строка STEP-005 → `Выполнено`.
- `planning/STATUS.md` — STEP-005 перенесён в `Recent completed`; `STEP-008`/`STEP-009` теперь unblocked (их единственная незакрытая hard dependency была STEP-005) — добавлены в `Next unblocked work`.
- `docs/requirements/SPEC.md` — `REQ-001` статус `Запланировано → Частично`: командная поверхность и pre-dispatch валидация реализованы, но полное REQ-001 Acceptance («результат выполнения отображается пользователю без ручного открытия файлов») зависит от реального agent invocation, которое явно вне scope STEP-005 (`NotImplementedAgentDispatcher`) и придёт со STEP-009.

## Handoff

STEP-005 закрыт (`Выполнено`). Следующая работа по roadmap — `PLAN STEP-006`/`STEP-007` (уже разблокированы) либо `PLAN STEP-008`/`STEP-009` (разблокированы этим закрытием).
