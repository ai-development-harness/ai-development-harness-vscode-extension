# REVIEW STEP-018 — 2026-09-19 17:29Z

**Reviewer role:** reviewer (независимый от автора ADR и remediation)
**Security review:** повторно выполнен из-за risk flag `security-sensitive`
**Verdict:** PASS
**Reviewed revision:** working tree (uncommitted), `main` @ `3646f5a`

## Scope checked

- Полный контракт `planning/tasks/STEP-018.md`: Scope, Mutation policy,
  Acceptance criteria, Implementation plan, Evidence и Review status.
- Закрытие F-001/F-002 из immutable review
  `planning/reviews/STEP-018/REVIEW-2026-09-19T1723Z.md`.
- REQ-005, ADR-004/ADR-007/ADR-008/ADR-009, ADR index, architecture
  baseline, STEP-009 и requirements/planning projections.
- Security boundary для credential/provider/proxy/certificate values и
  сохранение ADR-008 manual write fallback/read/containment contracts.
- Фактический working-tree scope. Review изменяет только этот immutable report.

## Closure предыдущего review

### F-001 — закрыт

- `planning/tasks/STEP-009.md:13-19` теперь включает ADR-009 в direct ADR
  traceability.
- `planning/tasks/STEP-009.md:391-398` содержит отдельный architecture
  handoff: следующий FIX не расширяет и не сохраняет передачу credential,
  provider, proxy или certificate variables, а оставляет только минимальные
  platform-home/runtime values без secrets.
- `docs/requirements/SPEC.md:152-155` связывает REQ-005 с STEP-018 и ADR-009;
  `docs/requirements/STATUS.md:11` сохраняет REQ-005 незавершённым и явно
  направляет runtime remediation в STEP-009.
- `docs/architecture.md:59-67` называет существующий environment allowlist
  architecture drift и однозначно требует удалить credential/provider/proxy/
  certificate forwarding в пользу user-managed CLI authentication.
- `docs/adr/ADR-008-headless-agent-write-boundary.md:7` и ADR index содержат
  reciprocal metadata о том, что ADR-009 supersedes только Decision 4. Текст
  исторического Decision не переписан и читается вместе с явным supersession.

В результате следующий `STEP FIX STEP-009` не может корректно выбрать прежнее
направление «расширить allowlist»: canonical task, REQ, architecture и ADR
metadata одинаково указывают на запрет передачи credential/provider values.

### F-002 — закрыт

- `planning/PLAN.md:24` отражает canonical статус STEP-018 `В работе`.
- `planning/STATUS.md:7-23` включает STEP-018 в architecture reconciliation,
  перечисляет его в `In progress`, фиксирует выполненную remediation и ставит
  повторный review перед corrective STEP-009.
- Статус `Выполнено` до PASS review не выставлялся.

## Findings

Новых blocking или non-blocking findings не обнаружено.

## Security observations

- ADR-009 не делает extension владельцем login/provider configuration:
  credential/provider/proxy/certificate values не читаются, не валидируются,
  не передаются и не логируются extension.
- User prerequisite остаётся безопасным: пользователь заранее настраивает и
  проверяет CLI в своём terminal; документация не содержит token/API-key
  values и не предлагает сохранять их в extension settings.
- ADR-009 supersedes только environment contract ADR-008. Automatic write
  по-прежнему запрещён, manual fallback не получает ложных containment/Ctrl+C
  гарантий, ADR-007 read-boundary не ослаблена.
- Текущий production allowlist честно обозначен как drift, подлежащий удалению
  только последующим STEP FIX STEP-009 с собственными tests/review; STEP-018
  не выдаёт документальное решение за уже реализованное runtime behavior.

## Acceptance observations

- Новый Accepted ADR однозначно запрещает extension передавать credential,
  provider, proxy и certificate environment variables и закрепляет
  user-managed authentication prerequisite.
- Historical ADR-008 Decision сохранён, а узкий supersession отражён в обоих
  ADR metadata и index; active contracts не противоречат друг другу.
- REQ-005, STEP-009, architecture, ADR index, PLAN и STATUS согласованы с
  ADR-009 и фактическим состоянием STEP-018.
- Remediation меняла только разрешённые documentation/planning artifacts.
  Production/test/settings mtimes предшествуют STEP FIX STEP-018; новых
  изменений этих поверхностей remediation не внесла.

## Verification observations

| Command/check | Exit | Observed |
|---|---:|---|
| `python3 tools/harness/validate.py --mode commit` | 0 | `HARNESS VALIDATION: PASS` для 309 tracked files; только warning об отсутствии staged files. |
| `git diff --check` | 0 | Пробельных ошибок нет. |
| Link existence check | 0 | REQ-005, STEP-009/017/018, ADR-004/007/008/009 и previous review существуют. |
| F-001 contract check | — | STEP-009/REQ/architecture/ADR metadata одинаково направляют FIX на удаление secret-bearing environment forwarding. |
| F-002 projection check | — | Task и PLAN — `В работе`; STATUS содержит remediation и повторный review handoff. |
| Scope/provenance check | — | Новых `src/**`, `tests/**`, dependencies или CLI settings изменений STEP-018/FIX не обнаружено. |

## Verdict rationale

Оба finding предыдущего review закрыты в canonical task, architecture,
requirements и projections. ADR-009 задаёт безопасную и однозначную ownership
boundary, сохраняет остальные контракты ADR-008/ADR-007 и честно оставляет
runtime drift следующему STEP-009 FIX. Acceptance criteria и verification
STEP-018 доказаны.

**Verdict: PASS.**

## Handoff

STEP-018 может быть завершён. Следующая corrective команда —
`STEP FIX STEP-009`: удалить credential/provider/proxy/certificate forwarding
по ADR-009, сохранить manual write fallback ADR-008 и закрыть остальные
актуальные findings STEP-009 перед новым независимым review.
