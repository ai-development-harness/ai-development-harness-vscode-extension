# REVIEW STEP-024 — 2026-09-20 14:18Z

**Reviewer role:** reviewer (третий независимый проход)
**Verdict:** FAIL
**Reviewed revision:** working tree (uncommitted), branch `chore/harness-v053-control-plane` @ `870e728`

## Scope checked

- `planning/tasks/STEP-024.md` целиком, включая оба раздела `STEP FIX STEP-024` и Review status.
- Предыдущие вердикты: `REVIEW-2026-09-20T1304Z.md` (цикл 1) и `REVIEW-2026-09-20T1403Z.md` (цикл 2).
- Каждое заявленное исправление второго fix-прохода — против фактического содержимого файлов
  и фактического существования целевых путей на диске и в `git ls-tree HEAD`.
- Полный свежий прогон Verification-последовательности с буквально захваченными exit code.
- Независимая mutation-проверка двух ключевых regression gates (не чтение кода).
- Полный повторный обход всех четырёх acceptance criteria, а не только findings цикла 2.
- Mutation scope второго fix-прохода по mtime + `git status`.
- Программная проверка существования всех backtick-путей в live project docs
  (`README.md`, `AGENTS.md`, `CLAUDE.md`, `docs/**` кроме `docs/adr/**`).

## Independent verification

Все команды выполнены ревьюером заново из корня репозитория; exit code захвачен буквально
(`echo "EXIT_<cmd>:$?"` сразу после команды, отдельным прогоном без pipe). «Литерально» —
точная цитата; остальное — нормализованное резюме.

| Команда | Exit | Наблюдение |
|---|---|---|
| `npm run compile` | 0 | литерально: только заголовок `tsc --noEmit`, без diagnostics |
| `npm run lint` | 0 | литерально: только заголовок `eslint src`, без warnings/errors |
| `npm run build` | 0 | литерально: `esbuild: build complete.` |
| `npm test -- --runInBand` | 0 | литерально (хвост): `Test Suites: 25 passed, 25 total` / `Tests: 327 passed, 327 total` |
| `npm run test:integration` | 0 | литерально (хвост): `27 passing (6s)`, `Exit code:   0` |
| `npm run test:integration:pre-activation` | 0 | литерально (хвост): `1 passing (683ms)`, `Exit code:   0` |
| `python3 .harness/tools/check-command-references.py --json` | 0 | `status: "DRIFT"`, **191** findings — расходится с заявленными в Evidence 169 (см. F-001) |
| `python3 .harness/tools/validate.py --mode manual` | 0 | литерально: `HARNESS VALIDATION: PASS (409 tracked files checked, mode=manual)` |
| `git diff --check` | 0 | пустой вывод |

## Проверка findings цикла 2

### F-001 цикла 2 (Medium, dangling `docs/harness/**`) — закрыт по четырём заявленным локациям

Проверено не по тексту правки, а по фактическому существованию целей
(`ls .harness/docs/` + `git ls-tree HEAD .harness/docs/` — все 22 файла присутствуют и
отслеживаются Git):

- `docs/development.md:60` → `.harness/docs/GIT_WORKFLOW.md` — файл существует;
- `docs/skills/README.md:9` → `.harness/docs/SKILL_MANAGEMENT.md` — существует;
- `docs/GLOSSARY.md:7` → markdown-ссылка `[`.harness/docs/GLOSSARY.md`](../.harness/docs/GLOSSARY.md)`
  — относительный путь из `docs/` резолвится в корневой `.harness/docs/GLOSSARY.md`, файл существует;
- `docs/README.md:19,23-25` → `.harness/docs/{README,DOCUMENT_MODEL,GLOSSARY}.md` — все три существуют.

Дополнительно `git show 870e728 --name-status -M` подтверждает переименования
`docs/harness/* → .harness/docs/*` (22 файла) и `planning/EXECUTION_PROTOCOL.md →
.harness/docs/EXECUTION_PROTOCOL.md`.

Полный повторный sweep по `docs/**`, `README.md`, `AGENTS.md`, `CLAUDE.md` показал ровно одну
оставшуюся живую ссылку на удалённый namespace — `docs/PROJECT.md:68`, см. F-003 ниже.
Остальные вхождения находятся в immutable history (`docs/adr/**`, `TZ_REVIEW_AND_PLAN.md`,
`planning/{reviews,harness-updates}/**`, `planning/tasks/STEP-001.md`,
`tests/fixtures/adr/ADR-001-*.md`) и переписываться не должны.

### F-002 цикла 2 (Low, drift в пяти live docs) — частично закрыт, с регрессией

Подтверждено: в `docs/PROJECT.md`, `README.md`, `docs/GLOSSARY.md`, `docs/README.md`,
`docs/architecture.md` — **0** findings, как и заявлено. Эта половина выполнена.

Однако общее число findings на текущем дереве — 191, а не 169. Разница ровно 22 — и все 22
находятся в `planning/tasks/STEP-024.md:315-335`, то есть внесены самим текстом Evidence,
который описывает это исправление. Подробно — F-001 ниже.

### F-003 / F-004 цикла 1 — по-прежнему закрыты

`npm run test:integration:pre-activation` присутствует в Verification и выполнен независимо
(exit 0). `.vscode-test.mjs` не в diff (mtime 13:13), его отложенный комментарий остаётся
явно зафиксированным в Evidence как сознательно не исправленный вне Allowed scope.

## Acceptance criteria — полный повторный обход

### Criterion 1 — compile/build без ссылок на `.project/**` — ВЫПОЛНЕН

`npm run compile` и `npm run build` — exit 0. Проверен собранный артефакт, а не только
исходники: `grep` по `dist/extension.js` даёт **0** вхождений
`.project/manifest`, `.project/command-transitions`, `.project/harness-config`; при этом
литерал `.harness/manifest.yaml` присутствует, CTS встроен в bundle.

### Criterion 2 — manifest-resolved / extension-owned paths — ВЫПОЛНЕН

Единственный locator — `HARNESS_MANIFEST_REL_PATH` (`src/parser/artifactPaths.ts:9`).
`grep -rn "\.project" src/` даёт только: поле schema `manifest.project.*`
(`src/commands/preDispatch.ts:19,22`, `src/parser/yamlParser.ts:54`), i18n-ключ
`projectConfiguration` и два комментария, явно фиксирующих отсутствие legacy layout.
Ни одного legacy path literal. `src/locales/activation.ts:24` резолвит
`.harness/harness-config.json` как extension-owned settings.

### Criterion 3 — regressions доказывают работу на `.harness/**` и отсутствие fallback — ВЫПОЛНЕН

Проверено независимой mutation-инъекцией в изолированной копии репозитория (рабочее дерево
не изменялось):

1. `HARNESS_MANIFEST_REL_PATH` → legacy литерал: `Tests: 6 failed, 174 passed` на подмножестве
   `tests/unit/{parser,explorer,editor}` — падает в том числе пин литерала
   `tests/unit/parser/artifactPaths.test.ts:29`. Gate реален, не тавтологичен.
2. В `parseManifest` добавлен настоящий fallback-`readFile` на соседний legacy layout перед
   возвратом `not-found`: `Tests: 1 failed, 7 passed` — падает ровно и только
   `tests/unit/parser/yamlParser.test.ts:46` («не читает legacy manifest как fallback»).
   Именно этот тест закрывает Forbidden-пункт Mutation policy.

Extension Host: `27 passing` + `1 passing` (pre-activation), exit 0 у обоих.

### Criterion 4 — документация отражает control-plane и текущий canonical command syntax — НЕ ВЫПОЛНЕН

Половина про control-plane paths выполнена (см. F-001 цикла 2 выше, плюс F-003 ниже как
остаточная локация). Половина про command syntax — нет: см. F-001 и F-002 ниже.

Immutable history не переписана — подтверждено: `docs/adr/**`,
`planning/{reviews,audits,harness-updates}/**`, `planning/tasks/STEP-001..023.md`,
`TZ_REVIEW_AND_PLAN.md` полностью вне `git status`.

## Mutation scope второго fix-прохода — соблюдён

По mtime, относительно времени цикла 2 (17:05 +03:00) изменены только:
`docs/development.md`, `docs/skills/README.md`, `README.md`, `docs/architecture.md`,
`docs/GLOSSARY.md`, `docs/README.md`, `docs/PROJECT.md` (17:07-17:08) и
`planning/tasks/STEP-024.md` (17:10). `src/**` целиком имеет mtime 15:55:21, тесты — 16:08-16:09
(цикл 1). `.harness/**` не изменён. Проход был docs-only, как и заявлено, и полностью внутри
Allowed mutation policy.

## Findings

### F-001 — Medium — Evidence фиксирует 169 findings, фактический прогон даёт 191; 22 self-referential drift-finding внесены самим текстом исправления

**Location:** `planning/tasks/STEP-024.md:315-335` (раздел «STEP FIX STEP-024, второй проход»,
описание F-002) и `planning/tasks/STEP-024.md:338-343, 360-362` (заявленное число).

**Scenario:**
Given цикл 1 уже зафиксировал ровно этот дефект (его F-002: Evidence приводил устаревший
command syntax открытым текстом в backticks, checker сканирует весь `planning/tasks/**`,
поэтому Evidence сам порождал 2 лишних finding) и исправление было сделано именно
переформулировкой без прямого цитирования;
When второй fix-проход описывает выполненную замену command labels и для наглядности
перечисляет обе стороны каждой замены в backticks — исходную и целевую форму — в
`planning/tasks/STEP-024.md:315-335`;
Then `check-command-references.py` засчитывает каждую исходную форму как drift в живом
mutable task-файле. Свежий прогон: `status: "DRIFT"`, **191** finding, из них **22** —
`planning/tasks/STEP-024.md` (строки 315, 321×2, 322, 323×2, 324×5, 325×3, 331, 332×2, 333×4,
335). 169 + 22 = 191: заявленное в Evidence число было верным ровно до момента, когда
Evidence с этим описанием был записан, и неверно для любого последующего прогона.

**Impact:** Дважды.
(1) AGENTS.md §8 требует, чтобы evidence соответствовало фактическому выводу команды. Число,
записанное в Evidence как результат deterministic gate, воспроизводимо не совпадает со свежим
прогоном той же команды — evidence в этой строке недостоверно, и §11 запрещает переводить STEP
в `Выполнено` на недоказанном criterion.
(2) `planning/tasks/STEP-024.md` — активный task-файл, а не immutable history; checker сканирует
`planning/tasks/STEP-*.md` намеренно (`.harness/tools/command_references.py`,
`project_live_document_paths`). По критерию, который выбрал сам fix-проход (вариант (а) —
исправлять drift в живых mutable документах), эти 22 строки относятся именно к живым, а не к
обоснованному immutable-остатку. Формулировка «остаток 169 — прежний pre-existing baseline в
historical/immutable документах» после этой правки неверна.
Регрессия, а не новая ошибка: тот же дефект был найден, признан и объявлен исправленным циклом
назад, теперь воспроизведён в 11× большем объёме. Runtime-влияния нет.

**Fix direction:** `STEP FIX STEP-024`, правка только `planning/tasks/STEP-024.md`: переписать
описание замен так, чтобы исходные (pre-namespace) формы не приводились дословно в backticks —
ровно тем приёмом, который цикл 1 уже применил успешно (описывать характер замены, а не
цитировать её левую часть). После этого пересчитать и зафиксировать в Evidence фактическое
число свежим прогоном, а не числом, полученным до записи самого Evidence. Ожидаемый результат —
169 и 0 findings в `planning/tasks/STEP-024.md`. `planning/reviews/**` checker не сканирует,
поэтому review-отчёты (включая этот) на число не влияют — это проверено эмпирически.

### F-002 — Low — Остаточный pre-namespace command syntax в трёх живых строках, включая строку, которую правил этот же проход

**Location:**
- `docs/skills/README.md:9` — перечисление skill-команд в форме `<ГЛАГОЛ> SKILL`;
- `docs/PROJECT.md:44` — та же форма в составе списка out-of-scope команд;
- `docs/PROJECT.md:25` — форма `<ГЛАГОЛ> STEP` для команды добавления шага.

**Scenario:**
Given criterion 4 требует, чтобы документация отражала текущий canonical command syntax, а
`AGENTS.md` §3 задаёт для этих команд namespaced формы `SKILL FIND` / `SKILL INSTALL` /
`SKILL CREATE` и `STEP ADD:`;
When второй fix-проход правит `docs/skills/README.md:9` — заменяя в **этой же строке** указатель
`docs/harness/…` на `.harness/docs/SKILL_MANAGEMENT.md` — и правит `docs/PROJECT.md:44`, приводя
семь из восьми перечисленных там команд к namespaced форме;
Then pre-namespace формы skill-команд остаются нетронутыми в обеих строках. `check-command-references.py`
их не отмечает (для этих словоформ в его таблице нет mapping), поэтому gate остаётся зелёным по
этим пяти файлам, но criterion шире gate. Форма `<ГЛАГОЛ> STEP` в `docs/PROJECT.md:25` —
единственная из трёх, которая явно объявлена отложенной в Evidence
(`planning/tasks/STEP-024.md:328-330`); две skill-строки не упомянуты нигде.

Это тот же паттерн «исправлена одна половина предложения», который цикл 2 зафиксировал как
свой F-001 — теперь по другому измерению той же строки `docs/skills/README.md:9`.

**Impact:** Только документационный, runtime не затронут. Но criterion 4 объявлен закрытым
«по обеим половинам» (`planning/tasks/STEP-024.md:367-371`), тогда как в живых project-owned
документах остаются формы, не соответствующие `AGENTS.md` §3. Читатель или агент, берущий
список команд из `docs/PROJECT.md`, получит две несуществующие формы.

**Fix direction:** Либо привести эти три строки к namespaced формам из `AGENTS.md` §3 (правка
docs-only, внутри Allowed scope, риск нулевой), либо явно зафиксировать их в Evidence как
сознательно отложенные — тем же способом, каким уже отложена форма из `docs/PROJECT.md:25`.
Выбор должен быть явным, а не следовать из того, что checker их не видит.

### F-003 — Low — `docs/PROJECT.md:68` ссылается на два пути, не существующие ни здесь, ни в репозитории, которому они атрибутированы

**Location:** `docs/PROJECT.md:68`, раздел «Референсы и внешние источники».

**Scenario:**
Given строка ссылается на соседний репозиторий `ai-development-harness-template` как на источник
структуры и называет в нём `planning/EXECUTION_PROTOCOL.md` и `docs/harness/COMMANDS.md`
(«полный список из 24 команд»);
When цикл 2 исключил эту строку из своего F-001 на основании того, что пути атрибутированы
соседнему репозиторию, а не этому;
Then проверка фактического состояния соседнего репозитория показывает, что это основание не
выполняется: `../ai-development-harness-template/docs/harness` и
`../ai-development-harness-template/planning/EXECUTION_PROTOCOL.md` **не существуют** — тот же
upstream relocation перенёс их в `../ai-development-harness-template/.harness/docs/`
(`COMMANDS.md` и `EXECUTION_PROTOCOL.md` присутствуют там). То есть это пятая локация того же
класса дефекта, исключённая по предположению, которое не подтвердилось.

**Impact:** Документационный. `docs/PROJECT.md` — canonical project overview и live current-state
документ (он объявлен `sources.projectOverview` в манифесте, файл правился в этом же проходе).
Ссылка на полный список 24 канонических команд — единственная в живых docs, и она не
резолвится ни в одном из двух репозиториев.

**Fix direction:** Заменить на `.harness/docs/COMMANDS.md` и `.harness/docs/EXECUTION_PROTOCOL.md`,
сохранив атрибуцию соседнему репозиторию (формулировка «источник структуры проекта» остаётся
верной). `docs/adr/**` и historical records не трогать.

## Наблюдение (не finding)

Семь комментариев в `src/**` (`src/commands/stepPicker.ts:14`, `src/locales/i18n.ts:11`,
`src/explorer/paths.ts:6,46`, `src/parser/types.ts:2,24`, `src/parser/yamlParser.ts:6`) ссылаются
на `ADR-001` как на действующее правило, тогда как `docs/adr/ADR-001-manifest-driven-paths.md:3,7`
— `Status: Superseded`, `Superseded by: ADR-005`, и этот же fix-проход отразил это в `AGENTS.md`.
Долг pre-existing (STEP-024 изменил в одном из них только путь), затрагивает семь модулей и не
относится к адаптации layout — по AGENTS.md §9 не должен исправляться «заодно». Кандидат в
отдельный `PROJECT QUICK FIX` или corrective STEP, вместе с уже отложенным комментарием в
`.vscode-test.mjs`.

## Specialized reviews

- Security: не требуется. Второй fix-проход — docs-only; trust boundary не менялся,
  известный path-traversal debt Parser/filesystem boundary по-прежнему честно зафиксирован
  в `docs/architecture.md` как открытый.
- Tests: отдельный test reviewer не запускался. Покрытие проверено этим проходом напрямую,
  включая независимую mutation-инъекцию двух ключевых gates; тесты со времени цикла 2 не менялись.

## Verdict rationale

Acceptance criteria 1-3 выполнены и доказаны deterministic gates: сборка чиста от legacy путей
на уровне собранного bundle, единственный locator подтверждён, отсутствие fallback доказано
mutation-инъекцией, а не чтением кода. Четыре dangling-ссылки на удалённый namespace из цикла 2
действительно исправлены — проверено существованием целевых файлов, а не текстом правки. Drift в
пяти названных живых документах действительно сведён к нулю. Mutation scope соблюдён, immutable
history не переписана, вся Verification-последовательность воспроизведена с exit 0.

Criterion 4 тем не менее остаётся недоказанным. Главное: deterministic gate этого criterion
возвращает 191, а Evidence фиксирует 169, и вся разница — 22 drift-finding, внесённые самим
текстом исправления в живой task-файл. Это буквальное повторение дефекта, который цикл 1 уже
нашёл, а fix-проход объявил устранённым, — в 11 раз большем объёме (F-001). Дополнительно в
живых docs остаются три строки pre-namespace синтаксиса, две из них — в строке, которую этот же
проход правил по другому поводу (F-002), и одна ссылка на удалённый namespace, исключённая
циклом 2 по предположению, которое фактическая проверка соседнего репозитория опровергает
(F-003).

Runtime-дефектов нет; весь остаток — docs-only и механический. Но по AGENTS.md §8 и §11 перевод
STEP в `Выполнено` при evidence, которое воспроизводимо расходится с фактическим выводом
собственной verification-команды, недопустим.

Verdict `FAIL`. Объём доработки: переформулировать описание замен в
`planning/tasks/STEP-024.md` и зафиксировать пересчитанное число (F-001); принять явное решение
по трём строкам pre-namespace синтаксиса (F-002); поправить `docs/PROJECT.md:68` (F-003).
После этого — новый независимый `STEP REVIEW STEP-024`.
