# REVIEW STEP-024 — 2026-09-20 14:03Z

**Reviewer role:** reviewer (второй независимый проход)
**Verdict:** FAIL
**Reviewed revision:** working tree (uncommitted), branch `chore/harness-v053-control-plane` @ `870e728`

## Scope checked

- `planning/tasks/STEP-024.md` целиком: Goal, Scope, Mutation policy, Out of scope,
  Acceptance criteria, Verification, Implementation plan, Evidence, Review status.
- Предыдущий verdict и его findings: `planning/reviews/STEP-024/REVIEW-2026-09-20T1304Z.md`
  (F-001 Medium, F-002 Low, F-003 Low, F-004 Low).
- Фактический diff `git diff` + untracked fixtures; `git ls-tree` на `HEAD` и `HEAD~1`
  для определения, что именно изменил upstream relocation `870e728`.
- Каждый новый/изменённый test case — на реальность gate, включая mutation-проверку
  в изолированной копии репозитория (не в рабочем дереве).
- Полная последовательность Verification, выполненная ревьюером заново.
- Baseline `check-command-references.py` на чистом worktree `HEAD`, с поэлементным
  сравнением findings (path/line/legacy), а не только количества.
- Mutation scope: какие файлы затронуты и принадлежат ли они Allowed списку;
  ownership по `.harness/harness-update.toml`.
- Doc sync (plan шаг 5) — не только по `.project/`, но и по всему namespace,
  удалённому тем же relocation.

## Independent verification

Все команды выполнены ревьюером заново из корня репозитория; exit code захвачен
буквально (`echo "EXIT_<cmd>:$?"` сразу после каждой команды). Тексты в колонке
«Наблюдение» — нормализованное резюме, если не помечено «литерально».

| Команда | Exit | Наблюдение |
|---|---|---|
| `npm run compile` | 0 | литерально: только заголовок `tsc --noEmit`, без diagnostics |
| `npm run lint` | 0 | без warnings/errors |
| `npm run build` | 0 | литерально: `esbuild: build complete.` |
| `npm test -- --runInBand` | 0 | литерально (хвост): `Test Suites: 25 passed, 25 total` / `Tests: 327 passed, 327 total` |
| `npm run test:integration` | 0 | литерально (хвост): `27 passing (6s)`, `Exit code:   0` |
| `npm run test:integration:pre-activation` | 0 | литерально (хвост): `1 passing (754ms)`, `Exit code:   0` |
| `python3 .harness/tools/check-command-references.py --json` | 0 | `status: "DRIFT"`, **192** findings |
| `python3 .harness/tools/validate.py --mode manual` | 0 | литерально: `HARNESS VALIDATION: PASS (409 tracked files checked, mode=manual)` |
| `git diff --check` | 0 | пустой вывод |

Транскрипт implementer'а не принимался на веру: все девять команд воспроизведены
независимо, цифры совпали (327 unit, 27 integration, 1 pre-activation, 192 drift).

## Проверка findings предыдущего review

### F-001 (Medium) — закрыт, подтверждено mutation-тестом

Пять новых unit-тестов существуют и не тавтологичны. Проверено не чтением, а
инъекцией дефекта в **изолированной копии** репозитория (рабочее дерево не менялось):

1. Мутация `src/parser/artifactPaths.ts:9` → `HARNESS_MANIFEST_REL_PATH = '.project/manifest.yaml'`:
   `Tests: 5 failed, 38 passed` — упали ровно 5 новых тестов и ни один старый:
   - `tests/unit/parser/artifactPaths.test.ts:29` — пин литерала `HARNESS_MANIFEST_REL_PATH`;
   - `tests/unit/explorer/paths.test.ts:17` — пин литерала `MANIFEST_REL_PATH`;
   - `tests/unit/parser/yamlParser.test.ts:46` — legacy-only tmp-каталог;
   - `tests/unit/explorer/refresh.test.ts:56` — `watchedPaths()` без `.project/`;
   - `tests/unit/editor/activation.test.ts:418` — `stepEditorWatchPatterns()` без `.project/`.
2. Мутация «настоящий fallback»: в `parseManifest` добавлен второй
   `readFile(manifestPath.replace('.harness', '.project'))` перед возвратом `not-found`.
   Результат: `Tests: 1 failed, 7 passed` — упал **только** новый тест
   `tests/unit/parser/yamlParser.test.ts:46`. То есть именно он, и только он,
   закрывает Forbidden-пункт «Fallback/probing/dual-layout `.project/**`».

Отдельно по вопросам из задания:
- Пин литерала `.harness/manifest.yaml` есть в двух местах (`HARNESS_MANIFEST_REL_PATH`
  и re-export `MANIFEST_REL_PATH`), сравнение идёт с буквальной строкой.
- `parseManifest` — фактическая boundary-функция (`src/parser/yamlParser.ts:11`),
  других locator/bootstrap функций в `src/parser/**` нет; тест кладёт в tmp-каталог
  **валидный** по schema legacy-манифест, поэтому `not-found` отличим от
  «нашёл, но не распарсил».
- Тавтология в `paths.test.ts` устранена корректно: строка 31 (бывшая 25) по-прежнему
  сравнивает `relPath` с константой, но сама константа теперь запинена на строке 17.
  Вместе эти два assertion'а образуют непрерывную цепочку до литерала.

Ограничение (не finding, зафиксировано как факт): тесты `watchedPaths`/
`stepEditorWatchPatterns` работают на манифестах, где `.project/` отсутствует по
построению, поэтому ловят только **захардкоженный** legacy-путь внутри самих функций.
Это и есть заявленный ими scope; основной gate — тест `parseManifest`.

### F-002 (Low) — закрыт, подтверждено поэлементно

`check-command-references.py --json` на текущем дереве: 192 findings. На чистом
worktree `HEAD` (`git worktree add --detach`, удалён после проверки): тоже 192.
Сравнение не только по количеству: множества ключей `(path, line, legacy)`
**совпадают полностью** (`current-only: []`, `baseline-only: []`). То есть это
буквально тот же pre-existing baseline, а не другое содержимое с тем же числом;
правки STEP-024 не сдвинули ни одной drift-строки. Findings из
`planning/tasks/STEP-024.md` отсутствуют — self-referential drift, дававший 194,
устранён переформулировкой Evidence.

Проверен и scope самого checker'а (`.harness/tools/command_references.py:162`):
сканируются `README.md`, manifest sources, `docs/**` кроме `docs/adr/**` и
`planning/tasks/STEP-*.md`; `planning/reviews/**` не сканируется — поэтому ни
предыдущий, ни этот отчёт на число findings не влияют.

### F-003 (Low) — закрыт

`npm run test:integration:pre-activation` присутствует в секции Verification
(`planning/tasks/STEP-024.md:73`) и в Evidence; выполнен ревьюером независимо:
exit 0, `1 passing (754ms)`.

### F-004 (Low) — корректно оставлен нетронутым

`.vscode-test.mjs` не входит в `git status` (mtime 13:13, до fix-прохода), его
комментарий про `.project/harness-config.json` на месте. В Evidence явно
зафиксировано как сознательно не исправленное вне Allowed scope (строки 262-265) —
не «тихо проигнорировано». Претензий нет.

### Production code при fix-проходе не менялся

`src/**` целиком имеет mtime 15:55:21 (+03:00), т.е. раньше времени первого review
(13:04Z = 16:04 +03:00); все файлы тестов — 16:08-16:09, STEP-файл — 16:13.
Содержимое `git diff -- src` совпадает с тем, что описал первый review (единственный
locator, 8 использований константы в пяти consumer-модулях). STEP FIX был test-only,
как и заявлено.

## Findings

### F-001 — Medium — Doc sync оставил висящие ссылки на удалённый namespace `docs/harness/**`; acceptance criterion 4 не выполнен

**Location:**
- `docs/development.md:60` — «Repository Git workflow задаётся `docs/harness/GIT_WORKFLOW.md` и `.harness/git-policy.toml`»;
- `docs/README.md:19,23-25` — раздел «Документация Harness» целиком описывает `harness/` как подкаталог `docs/`;
- `docs/GLOSSARY.md:7` — markdown-ссылка `[`harness/GLOSSARY.md`](harness/GLOSSARY.md)`;
- `docs/skills/README.md:9` — «Подробно: `docs/harness/SKILL_MANAGEMENT.md`».

**Scenario:** Given коммит `870e728` (`HARNESS UPDATE APPLY` v0.5.3) перенёс не только
control-plane данные, но и весь каталог Harness-документации: `git ls-tree HEAD~1`
показывает 22 файла `docs/harness/**`, на `HEAD` их нет, актуальное расположение —
`.harness/docs/**` (`.harness/docs/GIT_WORKFLOW.md`, `SKILL_MANAGEMENT.md`, `GLOSSARY.md`,
`README.md`, `DOCUMENT_MODEL.md` присутствуют);
When STEP-024 выполняет plan шаг 5 и правит `docs/development.md:60`, заменяя в **этом же
предложении** `.project/git-policy.toml` → `.harness/git-policy.toml`;
Then вторая ссылка того же предложения, `docs/harness/GIT_WORKFLOW.md`, остаётся указывать
на каталог, удалённый тем же самым relocation. Аналогично не синхронизированы
`docs/README.md` (раздел-оглавление Harness-документации), `docs/GLOSSARY.md` (битая
markdown-ссылка) и `docs/skills/README.md`. Проверено программно: из всех backtick-путей
в live project docs не существуют, помимо placeholder'ов (`STEP-NNN`), runtime-генерируемого
`.harness/harness-config.json` и ссылок на соседний template-репозиторий, именно эти четыре.

Первый review этого не поймал, потому что doc sync проверялся только grep'ом по
`\.project/`, а данный namespace называется иначе.

**Impact:** Acceptance criterion 4 («Документация отражает реализованный control-plane»)
фактически не выполнен: project-owned оглавление документации утверждает layout,
противоречащий реализованному control-plane, и даёт битые ссылки — включая ту самую,
через которую разработчик/агент ищет Git workflow этого репозитория. Runtime-дефекта нет.
По AGENTS.md §11 перевод STEP в `Выполнено` при недоказанном acceptance criterion
недопустим. Правка полностью внутри Allowed scope: `docs/**` не числится ни в
`harness_owned`, ни в `shared` (`.harness/harness-update.toml:34-63`), т.е. файлы
project-owned, mutable и не относятся к immutable history (`docs/adr/**`,
`planning/reviews|audits|harness-updates/**`).

**Fix direction:** `STEP FIX STEP-024`, docs-only: заменить `docs/harness/<FILE>.md` →
`.harness/docs/<FILE>.md` в `docs/development.md:60`, `docs/skills/README.md:9`,
`docs/GLOSSARY.md:7` и переписать раздел «Документация Harness» в `docs/README.md:19-25`
на `.harness/docs/**`. `docs/PROJECT.md:68` трогать не обязательно — там `docs/harness/COMMANDS.md`
явно атрибутирован соседнему репозиторию `ai-development-harness-template`; если правится,
то с сохранением этой атрибуции. `docs/adr/**` и historical records не переписывать.

### F-002 — Low — Обоснование «drift не исправляется» в Evidence покрывает 168 findings из 192; вторая половина criterion 4 осталась без решения

**Location:** `planning/tasks/STEP-024.md:64` (acceptance criterion 4, «…и текущий
canonical command syntax»); `planning/tasks/STEP-024.md:160-171` (Evidence, обоснование).

**Scenario:** Given criterion 4 требует, чтобы документация отражала текущий canonical
command syntax, а Verification включает `check-command-references.py`, который возвращает
`DRIFT`; When Evidence обосновывает отказ от исправления тем, что переписывание
historical STEP task files нарушило бы AGENTS.md §10;
Then это обоснование верно, но покрывает только `planning/tasks/STEP-001..020.md` —
168 findings из 192. Оставшиеся 24 приходятся на live, mutable, project-owned документы:
`docs/PROJECT.md` (18), `README.md` (2), `docs/GLOSSARY.md` (1), `docs/README.md` (1),
`docs/architecture.md` (1), и ещё 8 — на `planning/STATUS.md` и 9 — на
`docs/OPEN_QUESTIONS.md` (исторические записи evidence/Resolution, для них аргумент
immutability применим). Ни одна из 23 «живых» строк не защищена приведённым обоснованием
и не отмечена как сознательно отложенная.

**Impact:** Документационный, не функциональный. Но criterion 4 объявлен выполненным
при том, что его собственный deterministic gate возвращает `DRIFT`, а приведённое
объяснение не покрывает часть findings. Это делает criterion непроверяемым как gate и
переносит нерешённое решение в статус «выполнено».

**Fix direction:** Выбрать одно явно, а не по умолчанию: (а) в `STEP FIX STEP-024`
привести command syntax в живых `README.md`/`docs/PROJECT.md`/`docs/architecture.md`/
`docs/README.md`/`docs/GLOSSARY.md` к namespaced форме (23 строки, docs-only, внутри
Allowed scope), либо (б) явно сузить criterion 4 в `planning/tasks/STEP-024.md` до
control-plane paths и оформить отдельный `STEP ADD:` на command-syntax drift, зафиксировав
в Evidence остаток 192 = 168 immutable + 17 historical + 23 отложенных. Смешивать это с
переписыванием `planning/tasks/STEP-001..020.md` не нужно — там отказ обоснован верно.

## Проверенное и признанное корректным

- Acceptance criterion 1 выполнен: `npm run compile` и `npm run build` — exit 0,
  TS2307 отсутствует, `import commandTransitions from '../../.harness/command-transitions.json'`
  резолвится и встраивается esbuild'ом в bundle.
- Acceptance criterion 2 выполнен: единственный источник bootstrap path —
  `HARNESS_MANIFEST_REL_PATH` (`src/parser/artifactPaths.ts:9`); все consumer'ы
  (`src/commands/activation.ts:82`, `src/explorer/{activation.ts:74,104, paths.ts:47, refresh.ts:35}`,
  `src/editor/activation.ts:170,227,299`) импортируют константу. В `src/**` не осталось ни
  одного legacy path literal: совпадения на `.project` — поле schema `manifest.project.*`,
  i18n-ключ `projectConfiguration` и комментарии, явно фиксирующие отсутствие legacy layout.
- Acceptance criterion 3 выполнен: доказан не статически, а mutation-тестом (см. выше).
  Extension Host часть — `27 passing` на fixture-workspace c `.harness/**`, плюс
  `tests/integration/i18n.test.js` реально утверждает, что runtime пишет
  `.harness/harness-config.json` (падал бы при откате `resolveConfigPath`).
- `parseManifest` сохраняет fail-closed семантику: отсутствие манифеста → типизированный
  `err({kind:'not-found'})`, без probing и без чтения соседнего layout.
- Mutation scope соблюдён: изменены `src/**`, `tests/**`, `AGENTS.md`,
  `docs/{PROJECT,architecture,development}.md`, `planning/tasks/STEP-024.md`, `.gitignore`.
  `.harness/**` не изменён (кроме project-owned runtime execution state).
  `package.json`/`package.nls*.json` не трогались. `.gitignore` формально не перечислен в
  Allowed, но его единственная правка — путь ignored fixture-файла, механически обязательная
  при переносе fixtures; это минимальный supporting change по AGENTS.md §9, не scope creep.
- Новый ADR не требуется: адаптация к upstream layout, ADR-005 (manifest-first) в силе.
- Immutable history не переписана: `docs/adr/**`, `planning/reviews|audits|harness-updates/**`,
  `planning/tasks/STEP-001..023.md`, `TZ_REVIEW_AND_PLAN.md` вне diff.
- Evidence в STEP-файле теперь различает литеральный output и резюме и совпадает с
  независимо воспроизведёнными числами.

## Specialized reviews

- Security: не требуется. Trust boundary не менялся; известный path-traversal debt в
  Parser/filesystem boundary по-прежнему честно зафиксирован в `docs/architecture.md`
  как открытый и не выдан за закрытый.
- Tests: отдельный test reviewer не запускался — покрытие проверено этим проходом
  напрямую, включая mutation-проверку каждого нового gate.

## Verdict rationale

Все findings предыдущего прохода закрыты по существу, а не формально: пять новых
регрессионных тестов доказаны mutation-инъекцией (падают ровно они и только они),
drift-baseline подтверждён поэлементным сравнением с чистым `HEAD`, CI-gated suite
добавлена в Verification и выполнена, F-004 корректно оставлен вне scope. Production
code при fix-проходе не менялся. Acceptance criteria 1-3 выполнены и доказаны
deterministic gates.

Однако criterion 4 не выполнен ни в одной из двух своих половин. Doc sync пропустил
целый namespace, удалённый тем же самым relocation, к которому адаптируется STEP, —
причём в предложении, которое STEP-024 правил (F-001). А обоснование отказа от
command-syntax drift покрывает 168 findings из 192 и оставляет 23 строки в живых
mutable документах без решения (F-002). По AGENTS.md §11 перевод STEP в `Выполнено`
при недоказанном acceptance criterion недопустим.

Verdict `FAIL`. Объём доработки — docs-only, внутри Allowed scope, production code и
тесты не затрагиваются: `STEP FIX STEP-024` на четыре файла по F-001 плюс явное
решение по F-002 (исправить 23 строки либо формально сузить criterion 4 и вынести
drift в отдельный STEP).
