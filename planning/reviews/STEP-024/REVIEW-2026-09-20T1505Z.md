# REVIEW STEP-024 — 2026-09-20 15:05Z

**Reviewer role:** reviewer (четвёртый независимый проход)
**Verdict:** PASS
**Reviewed revision:** working tree (uncommitted), branch `chore/harness-v053-control-plane` @ `870e728`

## Scope checked

- `planning/tasks/STEP-024.md` целиком и заново, включая ревизию 2 Implementation plan
  (Scope / Mutation policy / Out of scope / Acceptance criteria / Verification переписаны после
  цикла 3) и все четыре подраздела Evidence.
- `planning/reviews/STEP-024/REVIEW-2026-09-20T1418Z.md` (цикл 3) — какие именно findings закрывались.
- Независимый пересчёт `check-command-references.py`, включая **независимое восстановление
  pre-STEP baseline** из чистого `HEAD`, а не принятие числа 192 из Evidence на веру.
- Фактическое содержимое и фактическое существование целей всех правок третьего fix-прохода.
- Полный прогон Verification-последовательности с буквально захваченными exit code.
- Независимая mutation-инъекция двух ключевых regression gates (criterion 3) — повторно, а не
  ссылкой на цикл 3.
- Полный обход всех четырёх acceptance criteria под **новой, суженной** формулировкой criterion 4.
- Mutation scope этого прохода по `git status` + mtime.
- Программный sweep backtick-путей и удалённых namespace в живых project-owned docs.

## Independent verification

Все команды выполнены ревьюером заново из корня репозитория; exit code захвачен буквально
(`echo "EXIT_<cmd>:$?"` сразу после команды в последовательном скрипте). «Литерально» — точная
цитата захваченного output; остальное — нормализованное резюме.

| Команда | Exit | Наблюдение |
|---|---|---|
| `npm run compile` | 0 | литерально: только заголовок `tsc --noEmit`, без diagnostics |
| `npm run lint` | 0 | литерально: только заголовок `eslint src`, без warnings/errors |
| `npm run build` | 0 | литерально: `esbuild: build complete.` |
| `npm test -- --runInBand` | 0 | литерально (хвост): `Test Suites: 25 passed, 25 total` / `Tests: 327 passed, 327 total` |
| `npm run test:integration` | 0 | литерально (хвост): `27 passing (6s)`, `Exit code:   0` |
| `npm run test:integration:pre-activation` | 0 | литерально (хвост): `1 passing (763ms)`, `Exit code:   0` |
| `python3 .harness/tools/check-command-references.py --json` | 0 | `status: "DRIFT"`, **169** findings — совпадает с Evidence |
| `python3 .harness/tools/validate.py --mode manual` | 0 | литерально: `HARNESS VALIDATION: PASS (409 tracked files checked, mode=manual)` |
| `git diff --check` | 0 | пустой вывод |

Все девять gates зелёные и совпадают с числами, зафиксированными в Evidence третьего прохода.

## Проверка findings цикла 3

### F-001 цикла 3 (Medium, self-referential drift в Evidence) — ЗАКРЫТ

Пересчитано независимо, а не принято из Evidence:

- Текущее дерево: `status: "DRIFT"`, **169** findings.
- Findings с `path == "planning/tasks/STEP-024.md"`: **0** (проверено фильтрацией JSON по полю
  `path`, не подсчётом по тексту).
- Распределение остатка по файлам: `docs/OPEN_QUESTIONS.md` (9), `planning/STATUS.md` (8),
  `planning/tasks/STEP-001..020.md` (152). Ни одного finding в живых project-owned docs.

**Baseline восстановлен независимо.** Вместо доверия числу 192 из Evidence выполнен
`git archive HEAD` в изолированный каталог (весь diff STEP-024 не закоммичен, поэтому `HEAD` —
корректный pre-STEP снимок) и прогон checker'а там: `status: "DRIFT"`, **192** findings.
Поэлементная разница baseline → текущее дерево:

| Файл | baseline | сейчас | delta |
|---|---|---|---|
| `docs/PROJECT.md` | 18 | 0 | −18 |
| `README.md` | 2 | 0 | −2 |
| `docs/GLOSSARY.md` | 1 | 0 | −1 |
| `docs/README.md` | 1 | 0 | −1 |
| `docs/architecture.md` | 1 | 0 | −1 |
| все прочие | 169 | 169 | 0 |
| **итого** | **192** | **169** | **−23** |

Ноль новых findings относительно зафиксированного baseline; из живых docs drift убран полностью.
Diff-gate из ревизии 2 Verification выполнен строго.

Дополнительно эмпирически подтверждено, что `planning/reviews/**` вне scan scope checker'а
(`.harness/tools/command_references.py:162-192`, docstring `project_live_document_paths`: reviews/
audits/releases/updates/searches не сканируются), поэтому этот отчёт на число findings не влияет.

### Текст Evidence — проверен на повторение self-referential дефекта

Прочитан весь раздел Evidence целиком. Описания замен третьего прохода и переписанный блок
второго прохода построены нейтрально: позиция описывается как `file:line` + характер правки
(«namespaced form per AGENTS.md §3»), целевая каноническая форма упоминается отдельно, пара
«старое → новое» в backticks больше не воспроизводится. Это соответствует инструкции шага 6
Implementation plan.

Два остаточных дословных упоминания pre-namespace форм всё же сохранились — см. L-1 ниже
(non-blocking, 0 findings).

### F-002 цикла 3 (Low, три живые строки pre-namespace syntax) — ЗАКРЫТ

Проверено по фактическому текущему содержимому файлов и сверено с канонической таблицей
`AGENTS.md` §3, а не по тексту Evidence:

- `docs/skills/README.md:9` — перечисление skill-команд приведено к namespaced форме
  (`SKILL FIND`, `SKILL INSTALL`, `SKILL CREATE`); в той же строке указатель на документацию —
  `.harness/docs/SKILL_MANAGEMENT.md`, файл существует (`ls` подтверждает). Обе половины строки,
  на рассинхрон которых указывал цикл 3, исправлены согласованно.
- `docs/PROJECT.md:25` — список 11 MVP-команд сверен поэлементно с `AGENTS.md` §3: все 11
  позиций совпадают с канонической формой, включая команду добавления шага, явно отложенную
  вторым проходом.
- `docs/PROJECT.md:44` — список out-of-scope команд сверен поэлементно: все 13 позиций совпадают
  с `AGENTS.md` §3, включая skill-группу, остававшуюся необработанной. 11 + 13 = 24, что
  согласуется с заявлением строки 68 про «полный список из 24 команд».

### F-003 цикла 3 (Low, `docs/PROJECT.md:68`) — ЗАКРЫТ

Проверено существование целей, а не текст правки:

- `../ai-development-harness-template/.harness/docs/EXECUTION_PROTOCOL.md` — существует;
- `../ai-development-harness-template/.harness/docs/COMMANDS.md` — существует.

Атрибуция соседнему репозиторию сохранена; формулировка «источник структуры проекта» осталась
корректной. Локальные аналоги (`.harness/docs/{EXECUTION_PROTOCOL,COMMANDS}.md`) также присутствуют.

## Acceptance criteria — полный повторный обход

### Criterion 1 — compile/build без ссылок на удалённый layout — ВЫПОЛНЕН

`npm run compile` и `npm run build` — exit 0. Проверен собранный артефакт, а не только исходники:
`grep` по `dist/extension.js` даёт **0** вхождений `.project/manifest`,
`.project/command-transitions`, `.project/harness-config`; литерал `.harness/manifest.yaml`
присутствует, CTS встроен в bundle.

### Criterion 2 — manifest-resolved / extension-owned paths — ВЫПОЛНЕН

Единственный locator — `HARNESS_MANIFEST_REL_PATH` (`src/parser/artifactPaths.ts:9`), ровно одно
определение в `src/**`. `grep -rn "\.project" src/` даёт только: поле schema `manifest.project.*`
(`src/commands/preDispatch.ts:19,22`, `src/parser/yamlParser.ts:54`), i18n-ключ
`projectConfiguration` (`src/locales/{en,ru}.json:45`) и два комментария, явно фиксирующих
отсутствие legacy layout (`src/api/commandPolicy.ts:2`, `src/parser/artifactPaths.ts:6`). Ни одного
legacy path literal. `src/locales/activation.ts:24` резолвит `.harness/harness-config.json` как
extension-owned settings.

### Criterion 3 — regressions доказывают работу на новом layout и отсутствие fallback — ВЫПОЛНЕН

Проверено **собственной** mutation-инъекцией в изолированной копии (`src`, `tests`, `node_modules`,
`jest.config.mjs`, `tsconfig.json` скопированы в scratchpad; рабочее дерево не изменялось):

1. `HARNESS_MANIFEST_REL_PATH` переведён на legacy литерал → на подмножестве
   `tests/unit/{parser,explorer,editor}`: `Test Suites: 5 failed, 14 passed`,
   `Tests: 6 failed, 174 passed, 180 total`. Gate реален, не тавтологичен; результат совпадает с
   независимым прогоном цикла 3.
2. В `parseManifest` (`src/parser/yamlParser.ts:14-19`) перед возвратом `not-found` внедрён
   настоящий fallback-`readFile` на соседний legacy layout → `Tests: 1 failed, 7 passed`, падает
   ровно и только тест `parseManifest › не читает legacy … как fallback (FIX STEP-024 F-001)`.
   Именно он закрывает Forbidden-пункт Mutation policy.

Extension Host: `27 passing` (основной suite) + `1 passing` (pre-activation), exit 0 у обоих.
`src/**` и `tests/**` не менялись с цикла 3 (mtime не новее 16:09 при правках этого прохода
в 17:41-17:44), поэтому регрессии criteria 1-3 исключены и подтверждены заново.

### Criterion 4 (ревизия 2, суженная формулировка) — ВЫПОЛНЕН

Применён критерий **из ревизии 2 плана**, а не расширенный стандарт циклов 1-3.

Обе его половины закрыты deterministic evidence:

1. **Пути в затронутых фрагментах.** Программный sweep по `README.md`, `AGENTS.md`, `CLAUDE.md`,
   `docs/**` (кроме `docs/adr/**`) на удалённые namespace (`.project/`, `docs/harness/`,
   `planning/EXECUTION_PROTOCOL`) даёт только объяснимые вхождения:
   - `AGENTS.md:304` — раздел §15 Harness self-update, описывающий обязательный bridge-переход
     со старого control plane; упоминание старого namespace здесь семантически обязательно;
   - `docs/OPEN_QUESTIONS.md:42,48,51,54` — историческая запись OQ-004 (resolved), явно
     перечисленная в Evidence как сознательно не переписываемая.

   Отдельный sweep всех backtick-путей в живых docs на фактическое существование дал пять хитов,
   каждый проверен и оказался объяснимым, не дефектом:
   `docs/README.md:8-9` — относительные ссылки внутри `docs/` (`docs/requirements/{SPEC,STATUS}.md`
   существуют); `docs/architecture.md:16` — `src/ui/statusBar.ts`, запланированный файл STEP-008;
   `docs/architecture.md:39` и `docs/development.md:30` — `.harness/harness-config.json`,
   runtime-генерируемый settings-файл (создаётся `activateI18n`, в fixture игнорируется через
   `.gitignore`), его отсутствие в repo ожидаемо.

2. **Command syntax в затронутых фрагментах.** Diff-gate выполнен строго: 169 против baseline 192,
   ноль новых findings, ноль findings в самом task-файле, ноль в живых project-owned docs
   (см. таблицу выше). Остаток 169 полностью лежит в `docs/OPEN_QUESTIONS.md`, `planning/STATUS.md`
   и `planning/tasks/STEP-001..020.md` — фрагментах, не затронутых control-plane-миграцией, что
   прямо отнесено к Out of scope ревизией 2.

**Оценка самой ревизии 2.** Сужение сформулировано корректно и разрешает ровно ту двусмысленность,
которая питала цикл: criterion 4 и Allowed mutation scope теперь согласованы по охвату, а gate
определён как diff относительно зафиксированного числа, а не как абсолютный ноль. Baseline 192
проверен ревьюером независимым восстановлением из `HEAD` и подтвердился точно, поэтому зафиксированное
в плане число не является непроверяемым самосогласованным утверждением. Претензий к формулировке нет.

Immutable history не переписана — подтверждено: `docs/adr/**`,
`planning/{reviews,audits,harness-updates}/**`, `planning/tasks/STEP-001..023.md`,
`TZ_REVIEW_AND_PLAN.md` полностью вне `git status`.

## Mutation scope третьего fix-прохода — соблюдён

По `git status` + mtime относительно времени цикла 3 (17:20 local) в этом проходе изменены ровно три
файла: `docs/skills/README.md` (17:41), `docs/PROJECT.md` (17:42), `planning/tasks/STEP-024.md`
(17:44). `src/**` — mtime не новее 15:55, `tests/**` — не новее 16:09; `.harness/**` не изменён.
Проход докуменационный, как и заявлено, и полностью внутри Allowed mutation policy.

Фактические diff'ы обоих docs-файлов прочитаны против `HEAD`: правки точечные, затрагивают только
целевые фрагменты, соседний контент не задет, семантика не изменена.

Отдельно оценён `.gitignore` (изменён ещё в первом проходе): правило игнорирования
runtime-генерируемого settings-файла fixture перенесено на новый layout. Формально `.gitignore`
не перечислен в Allowed списке, но это однострочное сопровождение переноса fixture из `tests/**`,
без которого fixture-состояние стало бы некорректным; квалифицируется как минимальный supporting
change по AGENTS.md §9. Не finding.

## Findings

Блокирующих findings нет.

### L-1 — Low (не блокирует) — два остаточных дословных упоминания pre-namespace форм в Evidence живут только за счёт особенности regex checker'а

**Location:** `planning/tasks/STEP-024.md:358` и `planning/tasks/STEP-024.md:432`.

**Scenario:**
Given шаг 6 Implementation plan прямо требует описывать замены без побуквенного воспроизведения
legacy-формы, поскольку Evidence входит в scan scope checker'а;
When третий fix-проход переписал основной блок нейтрально, но в двух местах сохранил legacy-форму
дословно в inline-code (строка 358 — форма команды добавления шага при объяснении, почему позиция
была отложена; строка 432 — skill-группа при описании, что именно правилось в `docs/PROJECT.md:44`);
Then checker их не засчитывает — но не потому, что формы нейтрализованы, а потому, что его паттерны
(`.harness/tools/command_references.py:37-40`) требуют lookahead `(?=[:\s])`, а в обоих местах за
словоформой идёт закрывающий backtick. Проверено напрямую: прогон тех же четырёх regex по файлу
даёт 0 совпадений, и полный прогон checker'а даёт 0 findings по этому пути.

**Impact:** Нулевой на текущем дереве: gate зелёный, число воспроизводимо, runtime не затронут.
Остаточный риск чисто латентный — если upstream Harness когда-либо ослабит lookahead в этих
паттернах, обе строки снова станут self-referential findings. `.harness/**` — Harness-owned и в
рамках этого STEP не меняется, поэтому в пределах его жизненного цикла рецидив невозможен.

**Fix direction:** Не требует отдельного `STEP FIX`. Достаточно при любой следующей правке этого
файла снять backtick-цитирование в двух строках, описав позиции так же, как остальной блок
(`file:line` + характер правки). Блокировать четвёртый цикл ради двух слов, не влияющих ни на
gate, ни на runtime, непропорционально.

## Наблюдения (не findings, перенесены из предыдущих циклов и подтверждены)

- `.vscode-test.mjs:12-13` — комментарий по-прежнему называет старый путь settings-файла, тогда как
  фактически резолвится `.harness/harness-config.json`. Файл вне Allowed mutation scope STEP-024,
  сознательное откладывание зафиксировано в Evidence (F-004 цикла 1). Кандидат в
  `PROJECT QUICK FIX`.
- Семь комментариев в `src/**` (`src/commands/stepPicker.ts:14`, `src/locales/i18n.ts:11`,
  `src/explorer/paths.ts:6,46`, `src/parser/types.ts:2,24`, `src/parser/yamlParser.ts:6`) ссылаются
  на `ADR-001` как на действующее правило, хотя `docs/adr/ADR-001-manifest-driven-paths.md:3,7` —
  `Status: Superseded`, `Superseded by: ADR-005`. Долг pre-existing, затрагивает семь модулей, не
  относится к адаптации layout; по AGENTS.md §9 не должен исправляться «заодно». Кандидат в тот же
  отдельный `PROJECT QUICK FIX` или corrective STEP вместе с предыдущим пунктом.

Оба пункта документационные, не влияют на поведение и явно зафиксированы — держать из-за них STEP
открытым оснований нет.

## Оценка риска бесконечного цикла

Механизм, питавший циклы 1-3, устранён на уровне контракта, а не разовой правкой:

1. Рассогласование criterion 4 и Allowed mutation scope, из-за которого ревьюер находил drift
   шире, чем implementer имел право править, закрыто ревизией 2 (сужение criterion + явный
   Out of scope пункт).
2. Абсолютный gate («ноль findings по репозиторию»), недостижимый без переписывания immutable
   history, заменён на diff-gate с числовым baseline, зафиксированным в плане и независимо
   восстановимым из `HEAD` — что и проделано этим проходом.
3. Самоподдерживающийся дефект (Evidence порождал findings, которые сам описывал) снят: текст
   переписан нейтрально, 0 findings в task-файле подтверждено фильтрацией по `path`.

Дальнейших источников расхождения между Evidence и фактическим выводом deterministic gates не
обнаружено. Оставшийся L-1 и два наблюдения — стабильные, документированные и не влияющие на
gates. STEP закрываем.

## Specialized reviews

- Security: не требуется. Третий fix-проход — docs-only; trust boundary не менялся. Известный
  path-traversal debt Parser/filesystem boundary по-прежнему честно зафиксирован в
  `docs/architecture.md:39` как открытый и не выдаётся за реализованную защиту.
- Tests: отдельный test reviewer не запускался. Покрытие проверено этим проходом напрямую, включая
  собственную mutation-инъекцию двух ключевых gates; `tests/**` со времени цикла 1 не менялись,
  все 327 unit + 27 + 1 Extension Host тестов зелёные.

## Verdict rationale

Все четыре acceptance criteria выполнены и доказаны deterministic gates, а не чтением кода.
Criteria 1-3 подтверждены заново, включая собственную mutation-инъекцию, которая воспроизвела
результаты цикла 3 точно: сборка чиста от legacy путей на уровне bundle, locator единственный,
отсутствие fallback падает ровно на целевом тесте.

Criterion 4 в его новой, суженной ревизией 2 формулировке выполнен. Ключевое: ревьюер не принял
baseline из Evidence на веру, а восстановил его независимо из чистого `HEAD` — получено ровно 192,
текущее дерево даёт ровно 169, поэлементная разница −23 приходится ровно на те пять живых
документов, которые правились, и ни на один другой файл. Ноль findings в самом task-файле
подтверждён фильтрацией JSON по `path`. Self-referential дефект, дважды рецидивировавший, закрыт
по существу. Все три finding цикла 3 проверены по фактическому состоянию файлов и существованию
целевых путей, а не по тексту описания правок, и закрыты.

Вся Verification-последовательность из девяти команд воспроизведена ревьюером с exit 0 и совпадает
с числами Evidence. Mutation scope прохода — три документационных файла, внутри Allowed policy;
immutable history не переписана.

Единственный остаток — L-1 (Low): два дословных упоминания legacy-словоформ в Evidence, которые
сейчас не порождают findings и не могут их породить, пока Harness-owned checker не изменён.
Блокировать четвёртый цикл ради формулировки, не влияющей ни на deterministic gate, ни на runtime,
ни на читаемость, было бы непропорционально для BUGFIX-STEP, прошедшего три fix-прохода.

Verdict `PASS`. Следующее действие — перевод `**Статус:**` STEP-024 в `Выполнено` отдельным шагом
(этот review lifecycle status не меняет), затем `GIT CHECK` → `GIT COMMIT`.
