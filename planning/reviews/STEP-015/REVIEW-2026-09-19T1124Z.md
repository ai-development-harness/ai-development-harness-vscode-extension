# REVIEW STEP-015 — 2026-09-19 11:24Z

**Reviewer role:** reviewer (независимый от implementer FIX F-009)
**Verdict:** FAIL
**Reviewed revision:** working tree (uncommitted), после FIX F-009

## Findings

### F-010 — Medium — дублирующиеся reference-поля обходят fail-closed delete guard

**Location:** `src/parser/markdownParser.ts:87-106`, consumers at `src/parser/markdownParser.ts:228-235`, `src/parser/markdownParser.ts:410-417`, `src/parser/markdownParser.ts:496-510`; guard filtering at `src/explorer/actions.ts:43-48`.

`extractBoldLabels()` и `extractLabeledBullets()` сохраняют значения в `Map`, поэтому повтор поля молча заменяет предыдущее значение. Например, пара `**Depends on:** STEP-009` и `**Depends on:** —` возвращает только placeholder без warning. Две строки `- STEP:` в Traceability REQ или ADR ведут себя так же. `canDelete()` не видит исходную ссылку на `STEP-009` и разрешает удаление.

**Impact:** F-009 закрыл повреждённые token внутри одного значения, но duplicate-label формат остаётся fail-open для destructive action и может оставить dangling reference.

**Fix direction:** выявлять дубликаты `Depends on` и `STEP` в Traceability как reference warning, который уже блокирует delete; так же fail-closed обработать повторяющиеся секции Traceability. Добавить parser/delete regressions для STEP, REQ и ADR.

### F-011 — Low — fixture `requirements/STATUS.md` расходится с canonical

**Location:** `docs/requirements/STATUS.md:8`, `tests/fixtures/requirements/STATUS.md:8`; контракт exact fixture sync — `planning/tasks/STEP-015.md:164`.

Canonical evidence обновлён после F-009, а fixture хранит старый текст о F-005/F-006. Exact comparison возвращает non-zero, несмотря на запись в Evidence FIX F-009 об успешной синхронизации.

**Fix direction:** синхронизировать fixture с canonical и повторить exact check.

## Previous findings

- **F-001..F-009 — CLOSED.** В частности F-009: смешанные корректные и повреждённые STEP token теперь дают reference warning и блокируют delete.

## Verification observations

| Command | Exit | Observed |
|---|---:|---|
| `npm run compile` | 0 | `tsc --noEmit` без ошибок. |
| `npm run lint` | 0 | ESLint без ошибок и предупреждений. |
| `npm run build` | 0 | `esbuild: build complete.` |
| `npm test` | 0 | 20 suites / 197 tests passed; только существующий warning `ts-jest` TS151002. |
| `npm run test:integration` | 0 | 15 passing в реальном headless VS Code Extension Host. |
| `git diff --check` | 0 | Пробельных ошибок нет. |

Security subreview не требуется: изменения не затрагивают security-sensitive scope; F-010 относится к correctness и fail-closed поведению destructive action.

## Verdict rationale

F-010 — воспроизводимый Medium fail-open defect destructive action; F-011 опровергает заявленную fixture-синхронизацию. STEP-015 остаётся `В работе`.

**Verdict: FAIL.** Handoff: `FIX STEP-015`.
