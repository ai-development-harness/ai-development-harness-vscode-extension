# REVIEW STEP-002 — 2026-09-17 17:46

**Reviewer role:** reviewer (independent, read-only относительно product code)
**Verdict:** FAIL
**Reviewed revision:** base commit `a480a81` (plan(step-002): зафиксируй инструментарий по реально проверенным версиям), working tree — незакоммиченные изменения IMPLEMENT/FIX-сессии (новые файлы scaffolding + правки `docs/development.md`, `.gitignore`, `planning/tasks/STEP-002.md`)

## Scope checked

- Полный текст `planning/tasks/STEP-002.md` (Goal, Context, Scope, Mutation policy, Out of scope, Acceptance criteria, Verification, Deliverables, Implementation plan, Evidence, Blocker).
- `planning/EXECUTION_PROTOCOL.md` §3, §4 (skim), §10 (полностью), §24, §25.
- Прочитаны: `package.json`, `tsconfig.json`, `eslint.config.js`, `esbuild.config.mjs`, `jest.config.mjs`, `.vscode-test.mjs`, `.vscode/launch.json`, `.nvmrc`, `src/extension.ts`, `tests/unit/`, `tests/integration/extension.test.js`, `.github/workflows/ci.yml`, `.gitignore`, `docs/development.md`.
- `git status`, `git log`, `git diff` по изменённым/новым файлам.
- Кросс-file consistency: `launch.json` `outFiles` vs фактический output `esbuild.config.mjs`; `jest.config.mjs` `roots`/`testMatch` vs фактическое содержимое `tests/unit/`; `.vscode-test.mjs` `files` glob vs фактический путь/расширение `tests/integration/extension.test.js`; extension id в smoke-тесте vs `publisher`+`name` в `package.json`.
- Независимый повторный прогон verification, включая полный чистый цикл (`rm -rf node_modules && npm ci`): `npm ci`, `npm run compile`, `npm run lint`, `npm run build`, `npm test`, `npm run test:integration` (реальный headless VSCode Extension Host).
- Сверка версий установленных зависимостей с `package.json` (eslint, jest, ts-jest, mocha, esbuild, typescript, `@vscode/test-cli`, `@vscode/test-electron`, `@typescript-eslint/parser`).
- Проверка истории `.nvmrc`/`.gitignore` (`git check-ignore -v`, `git status --short`) — global vs project-level exclude.
- Проверка, что product-логика (commands/explorer/editor) не затронута — Mutation policy Forbidden соблюдён.

## Findings

**F-001 (non-cosmetic, блокирует закрытие). `docs/development.md` содержит устаревшее и фактически неверное утверждение про `.vscode/launch.json`.**

- Файл: `docs/development.md`, секция `## Extension Development Host` (строка 43).
- Текущее содержимое: «`.vscode/launch.json` пока не создан ... F5-запуск Extension Development Host из VSCode UI не настроен на этом этапе.»
- Фактическое состояние репозитория: `.vscode/launch.json` существует (валидный JSON, конфигурация `extensionHost`, `preLaunchTask: "npm: build"`), создан пользователем и задокументирован в Evidence STEP-002 п.9 как уже созданный и проверенный.
- Почему это важно: `docs/development.md` — явный Deliverable STEP-002 («Обновлённый `docs/development.md` (реальные команды вместо TBD)»), и сам текст Evidence утверждает, что этот файл обновлён. На деле он не синхронизирован с последующей правкой (добавление `launch.json` произошло уже после того, как этот раздел `docs/development.md` был написан в исходном IMPLEMENT-проходе, и не был пересмотрен). Контрибьютор или агент, читающий этот документ сейчас, будет введён в заблуждение — ровно тот случай, который protocol §25 закрывает явным требованием «affected docs/status projections синхронизированы» как обязательным условием закрытия STEP, а §24 отдельно требует конкретности evidence, а не устаревших/общих формулировок.
- Тривиально исправимо: переписать абзац, отразив, что `launch.json` создан и F5 сконфигурирован.

Других findings нет. Отдельно проверено и признано корректным:

- `.vscode-test.mjs` `files: 'tests/integration/**/*.test.js'` корректно соответствует фактическому `tests/integration/extension.test.js`.
- `jest.config.mjs` `roots`/`testMatch` корректно соответствуют пустому `tests/unit/` (только `.gitkeep`), `passWithNoTests: true` присутствует и работает так, как заявлено в Evidence.
- `launch.json` `outFiles: ["${workspaceFolder}/dist/**/*.js"]` корректно соответствует реальному `esbuild.config.mjs` (`outfile: 'dist/extension.js'`).
- Extension id `ai-development-harness.harness-navigator` в smoke-тесте корректно соответствует `publisher`+`name` в `package.json`.
- Product-логика (commands/explorer/editor) не затронута — `src/extension.ts` действительно no-op stub; Mutation policy Forbidden соблюдён.
- `npm audit` (4 уязвимости: 2 low/1 moderate/1 high, транзитивные через `@vscode/test-cli`→mocha) честно и конкретно раскрыты в Evidence, соответствуют реальности, корректно квалифицированы как non-blocking dev-only tech debt.
- Интеграционный smoke-тест содержателен: резолвит extension по id, вызывает `ext.activate()`, проверяет `isActive === true` — реально упал бы, если бы `activate()` бросил исключение или завис.

## Verification observations

Все команды перезапущены независимо, включая полный чистый цикл:

```
$ rm -rf node_modules && npm ci
added 584 packages, and audited 585 packages in 6s
4 vulnerabilities (2 low, 1 moderate, 1 high)
exit=0
```
(Evidence указывает «added 581 packages» из `npm install`; мой `npm ci` показывает 584 — тривиальная вариация механики installer/lockfile, не расхождение по существу; audit-находки совпадают точно.)

```
$ npm run compile   → tsc --noEmit, exit=0, 0 ошибок
$ npm run lint      → eslint src, exit=0, 0 ошибок/warnings
$ npm run build     → esbuild: build complete. dist/extension.js + .map созданы, exit=0
$ npm test          → No tests found, exiting with code 0, exit=0
```
Всё совпадает с Evidence п.3–7 точно.

```
$ npm run test:integration
✔ Validated version: 1.138.0
Loading development extension at .../ai-development-harness-vscode-extension
    ✔ extension activates without throwing (58ms)
  1 passing (63ms)
Extension host with pid ... exited with code: 0
exit=0
```
Совпадает с Evidence п.8 (реальный download VSCode 1.138.0, headless-прогон, один проходящий smoke-тест; расхождение в таймингах 58/63ms vs заявленных 98ms — обычная вариация, тот же результат).

```
$ git status --short .nvmrc → ?? .nvmrc
$ git check-ignore -v .nvmrc → .gitignore:31:!.nvmrc  .nvmrc
```
Подтверждает историю `.nvmrc`/`.gitignore` из Evidence п.10 точно — глобальный `~/.gitignore` действительно игнорирует голый `.nvmrc`, а project-level `!.nvmrc` корректно это переопределяет; файл виден git как трекаемый/untracked, не игнорируется.

`.github/workflows/ci.yml` (`xvfb-run -a npm run test:integration`) в реальном CI не запускался — это и не заявлено как Evidence. `xvfb-run` локально недоступен, GitHub-hosted `ubuntu-latest`/`ubuntu-24.04` runner образ включает `xvfb` — правдоподобно, но не подтверждено эмпирически; не поднимаю как finding, так как Evidence этого не утверждает.

## Acceptance criteria checklist

| # | Критерий | Статус | Основание |
|---|---|---|---|
| 1 | `npm install` и сборка проходят без ошибок на чистом клоне | **Выполнено** | Независимо воспроизведено через `rm -rf node_modules && npm ci` (exit 0), далее `npm run compile`/`lint`/`build` (все exit 0) |
| 2 | Extension Development Host запускается (F5) без ошибок активации | **Выполнено (через раскрытый эквивалент)** | `.vscode/launch.json` существует и структурно корректен (`outFiles` соответствует esbuild output); буквальный интерактивный F5 не прогонялся (нет доступа к UI, честно раскрыто в Evidence/Blocker), но `npm run test:integration` независимо воспроизвёл реальный Extension Host, активирующий extension без ошибок — материально эквивалентная автоматическая проверка |
| 3 | Тестовый раннер запускается без ошибок конфигурации (даже с нулевым числом тестов) | **Выполнено** | `npm test` → «No tests found, exiting with code 0» (воспроизведено); `npm run test:integration` → 1 passing, exit 0 (воспроизведено) |
| 4 | Линтер запускается без конфигурационных ошибок | **Выполнено** | `npm run lint` → exit 0, 0 ошибок/warnings (воспроизведено с чистой установки) |

Все четыре acceptance criteria независимо подтверждены как выполненные. Единственный открытый finding (F-001) — вопрос completion gate (§25 doc sync), а не провал acceptance criterion.

## Specialized reviews

- Security: не требуется отдельно — Risk flags STEP-002 = `none`, весь diff — dev tooling/config, никакой production-логики. `npm audit` находки уже честно раскрыты в Evidence, не требуют отдельного security-review на этом STEP.
- Tests: not required отдельно — покрыто основным review (product test suite — предмет STEP-011).

## Verdict rationale

Каждое техническое/функциональное утверждение в Evidence независимо воспроизведено и подтверждено точным — включая нетривиальные, специфичные детали (точные числа `npm audit`, история `.nvmrc`/global-gitignore, реальный headless-прогон VSCode 1.138.0, исправление `passWithNoTests`). Это добросовестно выполненный и честно задокументированный STEP без correctness-багов, без нарушений Mutation policy и без скрытых отклонений от плана. Однако `docs/development.md` — явный Deliverable этого STEP — на данный момент содержит неверное утверждение о текущем состоянии репозитория («`.vscode/launch.json` пока не создан»), потому что этот раздел был написан в исходном IMPLEMENT-проходе до последующей амендмент-сессии, добавившей `launch.json`, и не был пересинхронизирован. Protocol §25 явно требует «affected docs/status projections синхронизированы» как обязательное условие закрытия STEP, а §24 отдельно отвергает неконкретную/устаревшую evidence. Поскольку сам текст Evidence STEP-002 утверждает, что `docs/development.md` обновлён, а по факту он сейчас неточен, это конкретный, воспроизводимый finding, а не cosmetic-only замечание — не проходит completion bar в текущем виде. Исправление тривиально (один абзац в `docs/development.md`) через `FIX STEP-002`, после чего STEP должен пройти PASS на повторном review без необходимости перепроверять остальное.

**Verdict: FAIL.**
