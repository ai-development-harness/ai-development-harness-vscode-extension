# REVIEW STEP-016 — 2026-09-19 09:47Z

**Reviewer role:** reviewer
**Verdict:** FAIL
**Reviewed revision:** working tree (uncommitted), `main`; architect/decision phase из `planning/tasks/STEP-016.md`

## Scope checked

- Контракт STEP-016: Scope, Mutation policy, Acceptance criteria, Implementation plan, Evidence и handoff.
- ADR-001, новый ADR-005 и ADR index; сохранение исторического тела ADR-001 и корректность supersession metadata.
- `docs/architecture.md`, `docs/OPEN_QUESTIONS.md` (`OQ-004`), REQ-002/REQ-003 и их projections.
- `planning/PLAN.md`, `planning/STATUS.md`, canonical tasks STEP-007/STEP-015/STEP-016 и review handoff STEP-015.
- Фактический diff, разрешённые fixture mirrors и прямые parser assertions.
- Контракт ADR-005: precedence, generation gate, no-probing, authoritative explicit target, existence/type check, local degradation, ownership и shared manifest ownership.

## Findings

### F-001 — Medium — canonical STEP-007 не содержит обязательную ссылку на ADR-005

**Location:** `planning/tasks/STEP-007.md:13-16`; встречные ссылки: `docs/adr/ADR-005-artifact-path-resolution.md:100-103`, `docs/requirements/SPEC.md:99-102`, `planning/tasks/STEP-016.md:39-40,137-146`.

**Scenario:** следующий `PLAN STEP-007` по execution protocol восстанавливает контракт прежде всего из canonical task, связанных REQ и ADR. STEP-007 уже получил hard dependency `STEP-016`, но его собственная секция `## ADR` по-прежнему содержит только ADR-002, а Context описывает только labeled-markdown contract. При этом ADR-005 и REQ-003 объявляют STEP-007 прямым потребителем общего resolver. Planner, следующий task-файлу, не получает прямой STEP → ADR-005 traceability и может снова спроектировать собственную derivation для ADR autocomplete.

**Impact:** не выполнена Acceptance criterion STEP-016 о согласованной REQ/STEP traceability и Scope-обязательство «для STEP-007 зафиксировать контракт». Разрыв воспроизводит риск третьего consumer-level path rule, ради устранения которого принят ADR-005. Наличие dependency на STEP-016 не заменяет явную связь STEP → ADR: STEP-016 является архитектурной задачей, а используемый устойчивый контракт — ADR-005.

**Fix direction:** в отдельном `FIX STEP-016` добавить ADR-005 в `planning/tasks/STEP-007.md → ## ADR` и кратко зафиксировать в Context, что autocomplete использует только neutral resolver ADR-005. Одновременно согласовать Mutation policy STEP-016: сейчас `planning/tasks/STEP-007.md` отсутствует в Allowed/Impacted files, хотя уже изменён для dependency и нужен для заявленной синхронизации. После исправления повторить consistency-check и независимый review.

## Confirmed contracts

- ADR-005 имеет `Status: Accepted`, `Supersedes: ADR-001`; ADR-001 получил только `Status: Superseded` и `Superseded by: ADR-005`, его исторические Context/Problem/Decision не переписаны.
- ADR index не оставляет ADR-001 и ADR-005 одновременно активными.
- Precedence определён однозначно: schema-known explicit manifest field → allowlisted derivation для поддерживаемой generation → unavailable.
- Для `harness.version: "1"` разрешены ровно `adrDirectory` и `requirementsStatus`; filesystem probing, global defaults и topology override через `.project/harness-config.json` запрещены.
- Explicit target authoritative: отсутствующий, нечитаемый или неверного типа explicit target не откатывается к derived candidate.
- Degradation локальна: отсутствие ADR directory не ломает Architecture, отсутствие requirements status не удаляет REQ nodes и не включает fallback на `SPEC.md`/`sources.status`.
- Долговременный owner — neutral Parser/path-resolution boundary; текущий Explorer-specific drift честно оставлен handoff'ом `FIX STEP-015` и не выдан за реализованный runtime contract.
- `.project/manifest.yaml` корректно описан как `ownership.shared` с Harness-managed schema.
- `OQ-004` переведён в `RESOLVED`, а STEP-015 остаётся заблокированным до PASS STEP-016 и отдельного FIX/REVIEW.

## Verification observations

Подтверждено root-agent для текущего diff:

| Проверка | Результат |
|---|---|
| Exact fixture sync (`cmp -s`) для SPEC, STATUS и ADR-001 | 3/3, exit 0 |
| Targeted parser suite | 18/18 tests passed |
| Full unit suite | 19 suites / 166 tests passed |
| `python3 tools/harness/validate.py --mode commit` | PASS, 275 tracked files |
| `git diff --check` | exit 0 |
| Pre/post checksum `src/**` | одинаков: `93c55e...` |
| `.project/manifest.yaml` SHA-256 | неизменён: `79373a...` |

Reviewer дополнительно повторил Harness validation и `git diff --check`: успешно; exact fixture pairs совпадают побайтно. Security/test subreview не требовался: factual STEP-016 delta документальный/архитектурный, production/runtime и новая test logic не изменялись.

## Verdict rationale

Содержание ADR-005 и supersession ADR-001 корректны и достаточны, deterministic gates зелёные, scope production/manifest сохранён. Однако canonical STEP-007 не связан с ADR-005, поэтому обязательная двусторонняя traceability и заявленный контракт будущего consumer не завершены. Это конкретный acceptance gap, а не косметическое замечание.

**Verdict: FAIL.** STEP-016 остаётся `В работе`. Handoff: `FIX STEP-016` → повторный независимый `REVIEW STEP-016`.
