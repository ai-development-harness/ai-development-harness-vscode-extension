# Harness update report

- **Initial release:** `v0.2.4` (из `.project/harness.lock.json`, `source.ref = v0.2.4`, immutable commit `fd3d6410b5165e7a5e15fd559682f03e131b612d`).
- **Requested final target:** latest (без `TO <tag>`), разрешён через canonical remote `.project/harness-update-graph.json` из `source.default_branch = main` → `latest = v0.2.7`.
- **Фактически пройденный route:** `v0.2.4 → v0.2.6 → v0.2.7`.
  - hop 1 `v0.2.4 → v0.2.6`, `kind=standard`, `reloadRequired=false`;
  - hop 2 `v0.2.6 → v0.2.7`, `kind=standard`, `reloadRequired=false`.
- **`reloadRequired` boundary:** отсутствует; весь route пройден одним запуском updater.
- **Пропуск `v0.2.5`:** не является skip обязательного hop. Canonical graph содержит прямой edge `v0.2.4 → v0.2.6`; `v0.2.5` имеет собственный outgoing edge `v0.2.5 → v0.2.6` и не лежит на маршруте из `v0.2.4`. Schema-инвариант «не более одного outgoing transition на каждый `from`» соблюдён.
- **Локальный `.project/harness-update-graph.json` до update** содержал `latest = v0.2.4` и не содержал новых edges; он использовался только как BASE-content (`harness_owned`), routing взят исключительно из remote `main`.

## Проверка graph (schema v1)

`schemaVersion == 1`; `latest` соответствует `source.tag_pattern` `^v\d+\.\d+\.\d+$`; у каждого transition есть `from`/`to`/`kind`/`reloadRequired`; `kind` ∈ {`standard`, `bridge`}; единственный `bridge` (`v0.1.1 → v0.1.2`) содержит непустой `reason`; `to` строго новее `from` во всех edges; каждый `from` встречается ровно один раз; циклов нет; route до `v0.2.7` существует и единственен.

Immutability route tags подтверждена: `v0.2.4` → `fd3d641`, `v0.2.6` → `2b53ff7`, `v0.2.7` → `1c69c25` (все `object.type = commit`).

## Policy transition

`.project/harness-update.toml` побайтово идентичен в `v0.2.4`, `v0.2.6` и `v0.2.7`, а local OURS совпадает с BASE. Ownership policy на всём маршруте не менялась.

- **Introduced managed paths:** нет (ни на одном hop).
- **Retired managed paths:** нет.
- **Ownership-reclassified paths:** нет.
- **`NEW_MANAGED_PATH_COLLISION`:** не обнаружено.
- **`OWNERSHIP_CLASS_CHANGE`:** не обнаружено.

Transition scope (union BASE ∪ THEIRS managed paths): 92 пути на hop 1, 91 на hop 2.

## Изменённые harness-owned пути (clean replace, OURS == BASE на каждом hop)

hop 1 (`v0.2.6`), 12 файлов:
`.agents/skills/documentation-sync/SKILL.md`, `.agents/skills/fix-step/SKILL.md`, `.agents/skills/implement-step/SKILL.md`, `.agents/skills/init-project/SKILL.md`, `.agents/skills/project-status/SKILL.md`, `.project/harness-update-graph.json`, `docs/harness/CI.md`, `docs/harness/DOCUMENT_MODEL.md`, `docs/harness/GETTING_STARTED.md`, `docs/harness/GLOSSARY.md`, `planning/EXECUTION_PROTOCOL.md`, `tools/harness/validate.py`.

hop 2 (`v0.2.7`), 4 файла:
`.agents/skills/reconcile-project/SKILL.md`, `.project/harness-update-graph.json`, `docs/harness/COMMANDS.md`, `planning/EXECUTION_PROTOCOL.md`.

Ни для одного `harness_owned` пути не обнаружено local modification — blocker не возникал.

## Shared / marker_merge (3-way merge)

- `.project/manifest.yaml` (`shared`) — 3-way merge на обоих hops, конфликтов нет. Изменено только `harness.release`: `0.2.4 → 0.2.6 → 0.2.7`. `harness.version` остался `"1"`. Секции `project:`, `language:`, `sources:`, `protocol:`, `repository:` не тронуты. `project.initialized` сохранён как `true` (update не менял состояние инициализации).
- `AGENTS.md` (`marker_merge`) — 3-way merge на hop 1, конфликтов нет. Upstream добавил два фрагмента: требование различать буквальный captured output и нормализованное резюме в разделе 8 (Deterministic gates) и уточнение про `docs/requirements/SPEC.md` / `docs/requirements/STATUS.md` в разделе 10. Локальные generated blocks `PROJECT-CONTEXT` и `SKILL-ROUTING` перенесены без изменений.
- `README.md` (`marker_merge`) — upstream-содержимое не менялось между `v0.2.4` и `v0.2.7`; локальные расхождения (включая блок `PROJECT`) сохранены как есть.
- `.gitignore` (`shared`) — upstream-содержимое не менялось на маршруте; локальные строки сохранены как есть.
- Остальные `shared` пути (`CLAUDE.md`, `.claude/settings.json`, `.claude/agents/**`, `.codex/config.toml`, `.codex/agents/**`, `.github/**`, `.gitmessage`, `.project/git-policy.toml`, `AGENTS.local.example.md`, `PROJECT_BRIEF.example.md`) — upstream-идентичны на маршруте, не тронуты.

## Lock

`.project/harness.lock.json` продвигался только после успешного postcondition соответствующего hop: `v0.2.4 → v0.2.6` (после hop 1), `v0.2.6 → v0.2.7` (после hop 2). Частично применённых hops не было.

## Verification evidence

- Pre-mutation validator: `python3 tools/harness/validate.py --mode manual` → `HARNESS VALIDATION: PASS (274 tracked/filesystem files checked, mode=manual)`.
- Pre-mutation working tree: `git status --porcelain` пуст (чистое дерево до update).
- Read-only моделирование всего маршрута `v0.2.4 → v0.2.6 → v0.2.7` выполнено до первой mutation; hop 2 моделировался против projected OURS после hop 1, а не против исходного дерева.
- Postcondition hop 1: все 12 replaced `harness_owned` файлов побайтово равны `v0.2.6`; `harness.release = 0.2.6`; marker-блоки `AGENTS.md` на месте.
- Postcondition hop 2: все 4 replaced `harness_owned` файла побайтово равны `v0.2.7`; `harness.release = 0.2.7`; required artifacts (`.project/harness-policy.toml`, `tools/harness/validate.py`, `docs/harness/COMMANDS.md`, `docs/harness/UPDATES.md`, `planning/EXECUTION_PROTOCOL.md`, `.project/harness.lock.json`) присутствуют.
- Синтаксическая валидность после mutation: `.project/harness.lock.json` и `.project/harness-update-graph.json` — валидный JSON; `.project/harness-update.toml` и `.project/harness-policy.toml` — валидный TOML; `.project/manifest.yaml` — валидный YAML; `tools/harness/validate.py` — компилируется (`py_compile`).
- Новый validator из `v0.2.7` умышленно **не** запускался этим updater-ом (security boundary: verification нового tooling относится к последующему `GIT CHECK`, не к `UPDATE HARNESS`).
- Migration/install/bootstrap scripts из target releases не запускались.
- Project-owned/unknown пути не тронуты: product code (`src/`), tests, `planning/tasks/`, `docs/adr/`, `docs/requirements/`, `docs/PROJECT.md`, `docs/architecture.md`, `.vscode/`, `package.json`. Локальный ignored артефакт `tools/harness/__pycache__/` попадает под glob `tools/harness/**`, отсутствует в BASE и THEIRS и намеренно не трогался.
- STEP/REQ/ADR не создавались; commit/push/PR не выполнялись.

## Handoff

`GIT CHECK` → `COMMIT`.
