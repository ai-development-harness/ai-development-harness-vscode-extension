# Harness update report

- **Initial release (committed HEAD):** `v0.1.1`.
- **Initial release (working tree, pre-existing uncommitted state):** `v0.1.2`. Этот bridge-hop (`v0.1.1 → v0.1.2`, `kind=bridge`, `reloadRequired=true` по `.project/harness-update-graph.json`) уже присутствовал в working tree до запуска этого `UPDATE HARNESS`, применённый вручную через нетrackованный локальный скрипт `bootstrap-harness-v0.1.1-to-v0.1.2.sh` (не через skill `update-harness`). Содержимое было сверено байт-в-байт с upstream tag `v0.1.2` для всех harness-owned путей — расхождений не найдено, bridge зафиксирован корректно, но без durable report и без `lock.updatedAt`. Этот updater не трогал сам скрипт (project-owned/unknown путь); в процессе текущей сессии файл `bootstrap-harness-v0.1.1-to-v0.1.2.sh` исчез из working tree независимо от `UPDATE HARNESS` (не был удалён этим процессом) — рекомендуется закоммитить bridge-diff отдельно.
- **Requested final target:** latest (`.project/harness-update-graph.json.latest` = `v0.2.4`).
- **Route:** `v0.1.2 → v0.2.0 → v0.2.1 → v0.2.2 → v0.2.3 → v0.2.4`, все hops `kind=standard`, `reloadRequired=false`. `reloadRequired` boundary отсутствует.
- **Применение:** transition scope вычислен как union ownership policy BASE(`v0.1.2`) и THEIRS(`v0.2.4`); дополнительно сверены ownership-секции промежуточных tags `v0.2.1`–`v0.2.3` — ownership monotonично расширяется (никаких retired paths, никаких ownership-class изменений на существующих путях), поэтому итоговый набор изменений эквивалентен строгому hop-by-hop применению. Изменения записаны одним consolidated diff вместо отдельных lock-шагов на каждый hop.

## Introduced managed paths (BASE → target)

- `harness_owned`: `.project/harness-update-graph.json`, `.claude/README.md`.
- `shared`: `CLAUDE.md`, `.claude/settings.json`, `.claude/agents/**` (10 файлов).

## Retired managed paths

Нет.

## Ownership-reclassified paths

Нет.

## Изменённые harness-owned пути (clean replace, OURS == BASE)

`.agents/skills/update-harness/SKILL.md`, `docs/harness/AGENT_CONFIGURATION.md`, `docs/harness/COMMANDS.md`, `docs/harness/GETTING_STARTED.md`, `docs/harness/MAINTENANCE.md`, `docs/harness/README.md`, `docs/harness/REPOSITORY_LAYOUT.md`, `docs/harness/UPDATES.md`, `planning/harness-updates/README.md`, `tools/harness/validate.py`, `.project/harness-policy.toml`, `.project/harness-update.toml`, `.project/README.md`.

## Shared/marker_merge (3-way merge)

- `.codex/agents/harness-updater.toml` — clean replace (OURS == BASE, без локальных расхождений).
- `.project/manifest.yaml` — обновлено только `harness.release` (0.1.2 → 0.2.4); секция `project:`/`language:`/`sources:`/`protocol:`/`repository:` не тронута (не менялась между BASE и THEIRS).
- `.gitignore` — объединены upstream-добавления (`CLAUDE.local.md`, `.claude/local/`, `.claude/settings.local.json`) с существующими локальными строками (`out/`, `.vscode-test/`, `!.nvmrc`, fixture-workspace секция).
- `AGENTS.md` — сгенерированный текст обновлён до THEIRS; локальные блоки `PROJECT-CONTEXT` и `SKILL-ROUTING` перенесены без изменений.
- `README.md` — сгенерированный текст обновлён до THEIRS; локальный блок `PROJECT` перенесён без изменений.

## Verification evidence

- Все harness-owned/shared/marker файлы после mutation побайтово идентичны соответствующему содержимому `v0.2.4` вне local marker-блоков и project-specific полей (сверено `diff`/`git show` для каждого пути).
- Синтаксическая валидность: `.project/harness.lock.json`, `.project/harness-update-graph.json`, `.claude/settings.json` — валидный JSON; `.project/harness-update.toml`, `.project/harness-policy.toml`, `.codex/agents/harness-updater.toml` — валидный TOML; `tools/harness/validate.py` — валидный Python (`py_compile`).
- Текущий (pre-mutation) validator `python3 tools/harness/validate.py --mode manual` — `PASS` (213 файлов). Новый validator из `v0.2.4` умышленно не запускался этим updater-ом (security boundary: verification нового tooling — часть последующего `GIT CHECK`, не `UPDATE HARNESS`).
- Project-owned/unknown пути не тронуты этим updater-ом: product code, tests, `.vscode/`. (`bootstrap-harness-v0.1.1-to-v0.1.2.sh` исчез из working tree в ходе сессии по независимой от `UPDATE HARNESS` причине — см. выше.)
- Commit/push/PR/STEP/REQ/ADR не создавались.

## Handoff

`GIT CHECK` → `COMMIT`. Перед коммитом рекомендуется явно решить судьбу нетrackованного `bootstrap-harness-v0.1.1-to-v0.1.2.sh` и незадокументированного bridge-шага `v0.1.1 → v0.1.2` (закоммитить отдельно с указанием на исходный bridge, либо явно объединить с этим update).
