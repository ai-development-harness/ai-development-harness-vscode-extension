# Обновление Harness до v0.5.3

## Результат

- Исходный release: `v0.4.2` (`f8a1a199bb3b596f1cf39cd94eec7aa14dfb500f`).
- Запрошенный и достигнутый target: `v0.5.3` (`e366c48777150a9e9d2f6d670d8976b8a8df2d5c`).
- Фактически пройденный маршрут: `v0.4.2 → v0.5.3`.
- Тип hop: `standard`; `reloadRequired: true`.

## Изменения ownership и layout

- Введён control-plane layout `.harness/**`.
- Перенесены разрешённые Harness-owned paths из `.project/**`, `docs/harness/**`, `tools/harness/**` и `planning/EXECUTION_PROTOCOL.md`.
- Shared `.project/manifest.yaml` и `.project/git-policy.toml` перенесены в `.harness/**` через 3-way merge; project-specific значения сохранены.
- `AGENTS.md` и `README.md` merged с сохранением project marker blocks.
- Старые source paths удалены только после создания destination state и `.harness/harness.lock.json`.

## Проверки

- Remote routing manifest: маршрут `v0.4.2 → v0.5.3` существует и является единственным.
- Immutable refs подтверждены через GitHub API.
- До mutation текущий validator: `PASS` (`393 tracked files`).
- После mutation `.harness/tools/validate.py --mode manual`: `PASS` (`393 tracked files`).
- Все shared и marker 3-way merges прошли без конфликтов; destination collisions отсутствовали.

## Граница выполнения

Этот hop требует reload нового Harness runtime. Текущий updater остановлен после успешного postcondition; continuation возможен только в новом запуске.
