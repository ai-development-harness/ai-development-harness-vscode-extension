# HARNESS UPDATE — 2026-09-20T11:10:52Z

## Результат

- Initial release: `v0.4.0`.
- Запрошенный конечный target: `v0.5.1`.
- Фактически применённый маршрут: `v0.4.0 → v0.4.1 → v0.4.2`.
- Статус: `UPDATER_RELOAD_REQUIRED`.

## Граница перезагрузки

Переход `v0.4.1 → v0.4.2` — bridge с `reloadRequired: true`. Он добавляет trusted bootstrap relocation semantics, необходимые для переноса control plane в `.harness/**`. Продолжение до `v0.5.1` в текущем runtime запрещено; после reload нужно повторить `HARNESS UPDATE CHECK TO v0.5.1 > APPLY`.

## Изменения маршрута

- `v0.4.0 → v0.4.1`: обновлены protocol documentation, validator, `reconcile-project` skill, CI workflow и command-reference tooling.
- `v0.4.1 → v0.4.2`: добавлены bootstrap relocation policy и fallback для update manifest; обновлены updater skill/runtime adapters, validation и документация maintenance.
- Новые managed paths: `tools/harness/check-command-references.py`, `tools/harness/command_references.py`.
- Retired и ownership-reclassified paths до границы reload отсутствуют.

## Проверки

- `python3 tools/harness/validate.py --mode manual` — PASS (`393 tracked files checked`).
- `git diff --check` — PASS.
- Проверка immutable refs `v0.4.0`, `v0.4.1`, `v0.4.2`, `v0.5.1` через GitHub API — PASS.
