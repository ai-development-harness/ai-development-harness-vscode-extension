# Harness update report

- **Initial release:** `v0.2.7` (из `.project/harness.lock.json`, `source.ref = v0.2.7`, immutable commit `1c69c254e0635bf69060fee20f164b29c5641b6a`).
- **Requested final target:** latest (без `TO <tag>`), разрешён через canonical remote `.project/harness-update-graph.json` из `source.default_branch = main` → `latest = v0.4.0`.
- **Фактически пройденный route:** `v0.2.7 → v0.3.0 → v0.4.0`.
  - hop 1 `v0.2.7 → v0.3.0`, `kind=standard`, `reloadRequired=false`;
  - hop 2 `v0.3.0 → v0.4.0`, `kind=standard`, `reloadRequired=true`.
- **`reloadRequired` boundary:** достигнут после `v0.4.0`; текущий updater завершается с `UPDATER_RELOAD_REQUIRED` и не выполняет дальнейшие действия новым protocol layer.

## Проверка graph (schema v1)

`schemaVersion == 1`; `latest = v0.4.0` соответствует `source.tag_pattern` `^v\d+\.\d+\.\d+$`; все transitions содержат обязательные поля; `kind` ограничен значениями `standard` и `bridge`; `bridge` содержит непустой `reason`; версии монотонно возрастают; каждый `from` имеет не более одного исходящего transition; циклов нет; route до `v0.4.0` существует и однозначен.

Route tags существуют как commit refs и разрешены в immutable commits: `v0.2.7` → `1c69c254e0635bf69060fee20f164b29c5641b6a`, `v0.3.0` → `c8b4e888be43e77d0ea29e9aef86d4c82531d337`, `v0.4.0` → `bfdd5ee310bccf7ab45248faf05ad91c7452621b`.

## Policy transition

Local `.project/harness-update.toml` до mutation побайтово совпадал с BASE `v0.2.7`. Policy `v0.3.0` идентичен BASE. Policy `v0.4.0` добавляет один `harness_owned` path: `.project/command-transitions.json`.

- **Introduced managed paths:** hop 1 — нет; hop 2 — `.project/command-transitions.json`.
- **Retired managed paths:** нет.
- **Ownership-reclassified paths:** нет.
- **`NEW_MANAGED_PATH_COLLISION`:** не обнаружено: `.project/command-transitions.json` отсутствовал и в BASE `v0.3.0`, и в projected OURS.
- **`OWNERSHIP_CLASS_CHANGE`:** не обнаружено.

Все изменённые `harness_owned` paths принимались только при `OURS == BASE`. Shared paths прошли 3-way merge без conflicts. В `.project/manifest.yaml` обновлён `harness.release`: `0.2.7 → 0.3.0 → 0.4.0`; `project.initialized` сохранён как `true`.

`AGENTS.md` и `README.md` применены через marker merge. Локальные blocks `PROJECT-CONTEXT`, `SKILL-ROUTING` и `PROJECT` после update побайтово совпадают с состоянием до mutation.

## Lock и postconditions

`.project/harness.lock.json` продвигался только после postcondition соответствующего hop:

1. После hop `v0.2.7 → v0.3.0` фактическое дерево совпало с заранее рассчитанным projected state, required Harness artifacts присутствовали, `harness.release = 0.3.0`; затем lock продвинут на `v0.3.0`.
2. После hop `v0.3.0 → v0.4.0` фактическое дерево совпало с заранее рассчитанным projected state, required Harness artifacts присутствовали, `.project/command-transitions.json` был валидным JSON, `harness.release = 0.4.0`; затем lock продвинут на `v0.4.0` с `source.ref = v0.4.0`.

Частично применённых hops и преждевременного продвижения lock не было.

## Verification evidence

- До первой mutation `git status --short --branch` показал clean working tree: `## main...origin/main`.
- Полный route `v0.2.7 → v0.3.0 → v0.4.0` read-only смоделирован до первой mutation; второй hop моделировался против projected OURS после первого hop.
- Pre-mutation validator: `python3 tools/harness/validate.py --mode manual` → `HARNESS VALIDATION: PASS (297 tracked files checked, mode=manual)`.
- Для обоих hops read-only модель не обнаружила merge conflicts, binary/non-UTF-8 managed files, ownership collisions или missing required files/skills/agents.
- Postcondition каждого hop подтверждён сравнением фактического дерева с соответствующим projected state; исключался только lock, который обновлялся отдельным действием после проверки.
- После финального hop JSON-файлы `.project/harness.lock.json`, `.project/harness-update-graph.json` и `.project/command-transitions.json`, TOML-файлы `.project/harness-policy.toml` и `.project/harness-update.toml`, а также YAML `.project/manifest.yaml` успешно разобраны как данные.
- Новый validator и другие target scripts из `v0.3.0`/`v0.4.0` updater-ом не запускались. Их выполнение относится к последующему `GIT CHECK` после reload и review diff.
- Migration/install/bootstrap hooks не запускались.
- Project-owned/unknown paths, STEP/REQ/ADR, Git refs и history не изменялись; commit/push/PR не выполнялись.

## Handoff

После reload: `GIT CHECK` → `COMMIT`.
