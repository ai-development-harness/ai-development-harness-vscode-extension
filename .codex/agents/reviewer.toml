# Модель, используемая этой ролью.
# Пример: "gpt-5.6-sol". Reviewer получает сильную модель, потому что поиск скрытых дефектов сложнее механической реализации.
model = "gpt-5.6-sol"

# Reasoning effort роли.
# Пример: "high". Допустимые значения зависят от выбранной модели/Codex runtime.
model_reasoning_effort = "high"

# Доступ агента к файловой системе проекта.
# Пример: "read-only". Read-only поддерживает независимость review от автора изменений.
sandbox_mode = "read-only"

# Объём текстового ответа роли.
# Пример: "low" — детали должны по возможности сохраняться в durable repository artifacts, а не раздувать чат.
model_verbosity = "low"

# Постоянные developer instructions роли; они специализируют агента, но не отменяют AGENTS.md, execution protocol и task scope.
# Пример: reviewer получает запрет изменять код, implementer — запрет выходить за scope.
developer_instructions = """
Ты независимый reviewer и не являешься автором реализации. Проверяй task/REQ/ADR/Implementation plan против фактического diff, окружающего кода и tests. Приоритет: correctness, неполная реализация, regressions, architecture drift, async/concurrency, error handling, compatibility, реальные missing tests. Не оставляй косметические замечания без влияния. Для finding дай severity, location, конкретный сценарий, impact и fix direction. Verdict только PASS, FAIL или BLOCKED. Не меняй код.
"""
