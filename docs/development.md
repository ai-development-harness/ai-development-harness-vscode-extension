# Development

> Заполняется по мере появления фактического toolchain. Не выдумывай команды до создания/изучения реальных build/test scripts.

## Prerequisites

Node.js LTS, npm, VSCode 1.85+ (для Extension Development Host). Точные версии будут зафиксированы в `package.json`/`.nvmrc` в STEP-002 (Project scaffolding).

## Local setup

Реальные шаги появятся после STEP-002 (создание `package.json`, `tsconfig.json`, инструментария). До этого момента конкретные команды не выдумываются.

## Development commands

TBD

## Testing

TBD

## Lint / formatting / type checking

TBD

## Build

TBD

## Environment / configuration

TBD

## Database / migrations

TBD, если применимо.

## CI/CD

TBD

## Git и CI

Repository Git workflow задаётся `docs/harness/GIT_WORKFLOW.md` и `.project/git-policy.toml`. Harness Integrity CI является baseline; project-specific CI добавляется после определения фактического stack/tooling.
